# อธิบายระบบโค้ดภาพรวม

เอกสารนี้ใช้เป็นแนวทางอธิบายโครงงานแบบแยกเป็นส่วน ๆ โดยเน้นให้เข้าใจง่าย ใช้ภาษาคน และเน้นอธิบาย `Controller` มากกว่า `ViewModel` กับ `View` เพราะ Controller เป็นส่วนที่แสดงการทำงานของระบบได้ชัดที่สุด

## แนวทางการอธิบายแต่ละหัวข้อ

ในแต่ละระบบให้ใช้โครงแบบนี้

1. ชื่อหน้า / ชื่อระบบ
2. ViewModel ที่ใช้
3. Controller
4. View
5. ผลลัพธ์ที่ผู้ใช้เห็น
6. ตารางฐานข้อมูลที่เกี่ยวข้อง

แนวการอธิบายที่แนะนำ

- `ViewModel` อธิบายสั้น ๆ ว่าใช้ตัวไหน และใช้ property อะไรเป็นหลัก
- `View` อธิบายสั้น ๆ ว่าฟอร์มหรือหน้าจอ bind กับอะไร และส่งไป action ไหน
- `Controller` อธิบายละเอียดที่สุด เพราะเป็นส่วนตรวจสอบข้อมูล ประมวลผล ติดต่อฐานข้อมูล และกำหนดผลลัพธ์ของระบบ
- `ผลลัพธ์ที่ผู้ใช้เห็น` ให้สรุปว่าผู้ใช้เห็นอะไรเมื่อทำรายการสำเร็จหรือไม่สำเร็จ
- `ตารางฐานข้อมูลที่เกี่ยวข้อง` ให้ระบุเฉพาะตารางที่มีบทบาทจริง

รูปแบบการแคปโค้ดที่แนะนำ

- `ViewModel` แคป 1 รูปพอ ถ้าไฟล์ไม่ยาว
- `View` แคปเฉพาะส่วนฟอร์มหรือส่วนแสดงผลที่เกี่ยวข้อง
- `Controller` ให้แคปตาม `ฟังก์ชัน` เป็นหลัก
- ถ้า 1 ฟังก์ชันยาวเกิน 1 รูป ให้แคปหลายรูปต่อเนื่องกัน
- หลังจากแปะรูปในส่วน Controller แล้ว ให้ `อธิบายรวมทีเดียว` ตามลำดับการทำงานของฟังก์ชัน
- ไม่จำเป็นต้องเขียนแยกว่ารูป 1 อธิบายอะไร รูป 2 อธิบายอะไร ถ้าเป็นฟังก์ชันเดียวกัน

ตัวอย่างแนวคิด

- `1 public IActionResult` หรือ `1 public method` สามารถมองเป็น `1 ฟังก์ชัน`
- ถ้าหน้านั้นมี `GET` และ `POST` ก็ให้แคปทั้ง 2 ฟังก์ชันในหัวข้อ Controller เดียวกัน
- ถ้าฟังก์ชันใดยาวมาก เช่นมี validation, query, save, redirect ครบในตัวเดียว ให้แคป 2-3 รูป แล้วอธิบายรวมเป็นข้อ ๆ ตาม flow

## ลำดับระบบที่แนะนำให้เริ่มอธิบาย

แนะนำให้อธิบายตามลำดับการใช้งานของผู้ใช้จริง จะทำให้ภาพรวมของระบบเข้าใจง่ายที่สุด

1. สมัครสมาชิก `Register / Signup`
2. เข้าสู่ระบบ `Login`
3. กรณีลืมรหัสผ่าน `Forgot Password + OTP`
4. จัดการข้อมูลส่วนตัว `Profile + Address`
5. หน้าแรกของระบบ `HomePage`
6. ดูสินค้าและโปรโมชัน `Menu / Promotion`
7. เพิ่มสินค้าเข้าตะกร้า `AddToCart`
8. จัดการตะกร้า `Cart / Update / Remove / Clear`
9. สั่งซื้อ `Checkout`
10. ติดตามคำสั่งซื้อ `MyOrders / OrderHistory`
11. ยกเลิกคำสั่งซื้อ / ยืนยันรับสินค้า / แนบสลิป
12. ส่วนหลังบ้าน `Dashboard / OrderMenu / Product / Account / Promotion`



---

## รูปแบบการอธิบายที่แนะนำ 2 แบบ

### แบบที่ 1 ภาพรวมของระบบ

ใช้เมื่อต้องการอธิบายโครงสร้างการทำงานของระบบแบบเข้าใจง่าย เห็นภาพรวมก่อนลงรายละเอียด โดยเน้นเรียงตามลำดับการใช้งานจริงของผู้ใช้ตั้งแต่เริ่มต้นจนจบกระบวนการ

หัวข้อที่เหมาะสำหรับแบบภาพรวม

1. ระบบสมาชิก
2. ระบบกู้คืนรหัสผ่าน
3. ระบบแสดงข้อมูลหน้าเว็บไซต์
4. ระบบจัดการโปรไฟล์และที่อยู่
5. ระบบสั่งซื้อสินค้า
6. ระบบชำระเงินและอัปโหลดสลิป
7. ระบบติดตามคำสั่งซื้อ
8. ระบบการแจ้งเตือน
9. ระบบจัดการออเดอร์ของพนักงาน
10. ระบบจัดการสต็อกของพนักงาน
11. ระบบจัดการสมาชิกของผู้ดูแล
12. ระบบจัดการสินค้าและหมวดหมู่ของผู้ดูแล
13. ระบบจัดการโปรโมชันของผู้ดูแล
14. ระบบแดชบอร์ดผู้ดูแล

### แบบที่ 2 ละเอียดตามระบบทั้งหมด

ใช้เมื่อต้องการอธิบายให้ครบตามฟังก์ชันจริงของระบบ โดยแยกตาม action หรือฟังก์ชันหลักที่มีอยู่จริงใน Controller ของฝั่ง User, Staff และ Admin

หัวข้อที่เหมาะสำหรับแบบละเอียด

#### ฝั่งผู้ใช้ทั่วไป (User / Customer)

1. `ระบบสมัครสมาชิก : (Signup)`
   ใช้สำหรับสร้างบัญชีผู้ใช้ใหม่ โดยรับข้อมูลจากฟอร์ม ตรวจสอบข้อมูลซ้ำ และบันทึกผู้ใช้ลงฐานข้อมูล
2. `ระบบเข้าสู่ระบบ : (Login)`
   ใช้สำหรับตรวจสอบตัวตนของผู้ใช้จากชื่อผู้ใช้หรืออีเมลและรหัสผ่าน จากนั้นสร้าง Session และพาไปยังหน้าตามบทบาท
3. `ระบบกู้คืนรหัสผ่าน : (ForgotPassword, RequestPasswordReset, VerifyPasswordResetOtp, ResetPassword)`
   ใช้สำหรับขอ OTP ยืนยันตัวตน และตั้งรหัสผ่านใหม่เมื่อผู้ใช้ลืมรหัสผ่าน
4. `หน้าแรกของระบบ : (Home)`
   ใช้แสดงสินค้าเด่น โปรโมชันสาธารณะ และข้อมูลเบื้องต้นของร้านในหน้าแรก
5. `หน้าเมนูสินค้า : (Menu)`
   ใช้แสดงรายการสินค้าทั้งหมดที่พร้อมขาย พร้อมข้อมูลราคา หมวดหมู่ และจำนวนคงเหลือ
6. `หน้าโปรโมชัน : (Promotion)`
   ใช้แสดงรายการโปรโมชันที่กำลังเปิดใช้งาน รวมถึงโปรโมชันกิจกรรมที่ผู้ใช้สามารถส่งคำขอรับสิทธิ์ได้
7. `หน้าข้อมูลติดต่อ : (Contact)`
   ใช้เชื่อมไปยังหน้าแรกของระบบ โดยในโปรเจกต์นี้ไม่ได้มีหน้าติดต่อแยกเฉพาะ
8. `ระบบจัดการโปรไฟล์ : (Profile)`
   ใช้สำหรับดูและแก้ไขข้อมูลบัญชีของผู้ใช้ รวมถึงอัปเดตข้อมูลส่วนตัวจากหน้าโปรไฟล์
9. `ระบบจัดการที่อยู่ : (SaveAddress, DeleteAddress, GetUserAddresses)`
   ใช้สำหรับเพิ่ม แก้ไข ลบ และดึงรายการที่อยู่ของผู้ใช้ไปใช้งานในหน้าโปรไฟล์และหน้า Checkout
10. `ระบบสั่งซื้อสินค้า : (Checkout, CreateOrder, GetCurrentUser)`
    ใช้สำหรับเตรียมคำสั่งซื้อ ตรวจสอบข้อมูลผู้ใช้ และบันทึกออเดอร์ลงฐานข้อมูล
11. `ระบบใช้งานโปรโมชันและขอรับโปรโมชัน : (GetUserPromos, SubmitPromotionClaim)`
    ใช้สำหรับดึงโปรโมชันของผู้ใช้ไปใช้ใน Checkout และส่งคำขอรับโปรโมชันกิจกรรมพร้อมหลักฐาน
12. `ระบบชำระเงิน : (Payment)`
    ใช้แสดงยอดชำระเงินและ QR Code สำหรับโอนเงินผ่าน PromptPay
13. `ระบบอัปโหลดสลิป : (UploadSlip)`
    ใช้รับไฟล์หลักฐานการชำระเงิน บันทึกไฟล์ และอัปเดตสถานะการชำระเงินของคำสั่งซื้อ
14. `ระบบติดตามสถานะคำสั่งซื้อ : (Delivery)`
    ใช้แสดงสถานะปัจจุบันของออเดอร์ ข้อมูลการจัดส่ง และประวัติคำสั่งซื้อที่เสร็จสิ้นแล้ว
15. `ระบบยืนยันได้รับสินค้า : (CompleteOrder)`
    ใช้สำหรับให้ผู้ใช้ยืนยันว่าได้รับสินค้าแล้ว และย้ายข้อมูลไปยังประวัติคำสั่งซื้อ
16. `ระบบการแจ้งเตือน : (GetMyNotifications, MarkAsRead, MarkAllAsRead, ClearMyNotifications)`
    ใช้สำหรับดึง อ่าน และล้างการแจ้งเตือนของผู้ใช้

#### ฝั่งพนักงาน (Staff)

1. `ระบบจัดการออเดอร์ : (Order, GetOrderDetails)`
   ใช้สำหรับดูรายการออเดอร์ อ่านรายละเอียดสินค้า และตรวจสอบข้อมูลการสั่งซื้อของลูกค้า
2. `ระบบยืนยันการชำระเงิน : (ConfirmPayment)`
   ใช้สำหรับตรวจสอบหลักฐานการชำระเงิน อัปเดตสถานะการจ่ายเงิน และตัดสต็อกสินค้าตามออเดอร์
3. `ระบบเตรียมสินค้าและจัดส่ง : (AcceptOrder, ShipOrder)`
   ใช้สำหรับรับออเดอร์เข้าสู่ขั้นตอนเตรียมสินค้า และเปลี่ยนสถานะเป็นจัดส่งเมื่อพร้อมส่ง
4. `ระบบจัดการสต็อกสินค้า : (Stock, UpdateStockSettings)`
   ใช้สำหรับดูจำนวนคงเหลือของสินค้า และอัปเดตสต็อกหรือสถานะพร้อมขายของสินค้า
5. `ระบบตรวจสอบคำขอโปรโมชัน : (EventPromotion, ApproveClaim, RejectClaim)`
   ใช้สำหรับตรวจสอบคำขอโปรโมชันจากลูกค้า พร้อมอนุมัติหรือปฏิเสธตามหลักฐานที่ส่งมา

#### ฝั่งผู้ดูแลระบบ (Admin)

1. `ระบบแดชบอร์ด : (Dashbordadmin)`
   ใช้สำหรับดูภาพรวมของระบบ เช่น ยอดขาย คำสั่งซื้อ สินค้าใกล้หมด และข้อมูลสรุปสำคัญของร้าน
2. `ระบบจัดการสมาชิก : (Member, SaveMember, DeleteMember)`
   ใช้สำหรับดู เพิ่ม แก้ไข และลบข้อมูลผู้ใช้ในระบบ รวมถึงกำหนดบทบาทของผู้ใช้
3. `ระบบจัดการสต็อกและหมวดหมู่ : (Stock, UpdateStockSettings, SaveCategory, SaveStock)`
   ใช้สำหรับจัดการข้อมูลสินค้าและหมวดหมู่ โดยแอดมินสามารถเพิ่มหรือแก้ไขสินค้า สร้างหรือแก้ไขหมวดหมู่ และปรับสต็อกสินค้าได้
   งานปรับสต็อกบางส่วนเป็นงานที่ทำร่วมกับฝั่ง staff
4. `ระบบจัดการโปรโมชัน : (PromotionAdmin, SavePromotion, GiftPromotion, GiftPromotionToAll)`
   ใช้สำหรับสร้าง แก้ไข บันทึก และมอบโปรโมชันให้ผู้ใช้รายบุคคลหรือผู้ใช้ทั้งหมดตามเงื่อนไขที่กำหนด



---

## ตัวอย่างการอธิบาย

## 1. หน้า Login

### 1. ViewModel ที่ใช้
หน้า Login ใช้ `AuthUserViewModel` เป็นตัวกลางในการรับข้อมูลจากฟอร์มเข้าสู่ระบบ โดยในหน้านี้ใช้งานหลักที่ `Username` และ `Password` แม้ว่า ViewModel ตัวนี้จะถูกใช้ร่วมกับหน้า Signup ด้วย แต่ในส่วน Login จะเน้นเฉพาะข้อมูลที่ใช้ตรวจสอบตัวตนของผู้ใช้

รูปที่ควรแปะ

- `Viewmodels/AuthUserViewModel.cs` บรรทัด 1-10

อธิบายใต้รูป

- `AuthUserViewModel` อยู่ในโฟลเดอร์ `Viewmodels`
- หน้า Login ใช้ property `Username` และ `Password`
- `Username` รองรับทั้งชื่อผู้ใช้และอีเมล
- `Password` ใช้เปรียบเทียบกับข้อมูลรหัสผ่านในฐานข้อมูล

### 2. Controller
การทำงานของหน้า Login อยู่ใน `AccountController` action `Login` ทั้งแบบ `GET` และ `POST`

รูปที่ควรแปะ

- `Controllers/AccountController.cs` บรรทัด 21-68
- ถ้าแคปไม่หมดในรูปเดียว ให้แบ่งเป็น 2 รูปต่อเนื่องกัน แต่ยังถือว่าเป็นการอธิบาย Controller ชุดเดียวกัน

ลำดับการทำงาน

1. ระบบเปิดหน้า Login โดยเรียก `GET Login()` เพื่อสร้าง `AuthUserViewModel` และส่งไปยังหน้า View
2. เมื่อผู้ใช้กรอก `Username` และ `Password` แล้วกดเข้าสู่ระบบ ข้อมูลจะถูกส่งกลับมายัง `POST Login(AuthUserViewModel model)`
3. ในส่วน `POST Login(...)` Controller จะตรวจสอบก่อนว่าผู้ใช้กรอกข้อมูลครบหรือไม่ หากกรอกไม่ครบจะเพิ่มข้อความผิดพลาดและส่งกลับไปหน้าเดิม
4. จากนั้นระบบจะค้นหาผู้ใช้จากตาราง `Users` โดยรองรับทั้งการกรอกชื่อผู้ใช้และอีเมล
5. เมื่อพบข้อมูลผู้ใช้แล้ว ระบบจะตรวจสอบว่ารหัสผ่านที่กรอกตรงกับข้อมูลในฐานข้อมูลหรือไม่
6. หากไม่พบผู้ใช้หรือรหัสผ่านไม่ถูกต้อง ระบบจะบันทึก log การเข้าสู่ระบบไม่สำเร็จ พร้อมแสดงข้อความ `Invalid username or password`
7. หากข้อมูลถูกต้อง ระบบจะบันทึกข้อมูลสำคัญของผู้ใช้ลง `Session` เช่น `UserId`, `Username`, `RoleId` และ `RoleName`
8. สุดท้ายระบบจะตรวจสอบสิทธิ์ของผู้ใช้ แล้ว redirect ไปยังหน้าที่เหมาะสมตามบทบาท ได้แก่ Admin ไป Dashboard, Staff ไป Order และ User ไป Home

ถ้าต้องการแปะเพิ่ม

- `Controllers/AccountController.cs` บรรทัด 249-266

อธิบายใต้รูป

- เมธอด `LogUserLogin(...)` ใช้บันทึกประวัติการเข้าสู่ระบบลงไฟล์ log
- ระบบจะเก็บชื่อผู้ใช้ สถานะการเข้าสู่ระบบ และ IP Address เพื่อใช้ตรวจสอบย้อนหลัง

### 3. View
หน้า Login อยู่ในไฟล์ `Views/Account/Login.cshtml` และใช้ `AuthUserViewModel` สำหรับรับข้อมูลจากฟอร์มเข้าสู่ระบบ

รูปที่ควรแปะ

- `Views/Account/Login.cshtml` บรรทัด 386-410

อธิบายใต้รูป

- หน้า View กำหนด `@model AuthUserViewModel`
- มีฟอร์มสำหรับกรอก `Username or Email` และ `Password`
- มีปุ่มเข้าสู่ระบบและลิงก์ไปยังหน้ากู้คืนรหัสผ่าน
- มีพื้นที่สำหรับแสดงข้อความผิดพลาดเมื่อเข้าสู่ระบบไม่สำเร็จ

### 4. ผลลัพธ์ที่ผู้ใช้เห็น

- ผู้ใช้จะเห็นหน้า Login พร้อมช่องกรอก `Username or Email` และ `Password`
- ถ้ากรอกข้อมูลไม่ครบหรือไม่ถูกต้อง ระบบจะแสดงข้อความแจ้งเตือนบนหน้าเดิม
- ถ้าเข้าสู่ระบบสำเร็จ ระบบจะพาไปยังหน้าถัดไปตามบทบาทของบัญชี
- ผู้ใช้ทั่วไปไปหน้า Home
- Staff ไปหน้าจัดการออเดอร์
- Admin ไปหน้า Dashboard

รูปที่ควรแปะ

- หน้า Login ก่อนกรอกข้อมูล
- หน้า Login ตอนแสดง error
- หน้าหลัง Login สำเร็จตาม role

### 5. ตารางฐานข้อมูลที่เกี่ยวข้อง

- `Users`
  ใช้ตรวจสอบข้อมูล `Username`, `Email`, `Password`, `RoleId`
- `Roles`
  ใช้กำหนดสิทธิ์ของผู้ใช้ผ่าน `RoleId`
- `wwwroot/logs`
  ใช้บันทึกประวัติการเข้าสู่ระบบในรูปแบบไฟล์ log

---

## 2. หน้า Signup

### 1. ViewModel ที่ใช้
หน้า Signup ใช้ `AuthUserViewModel` ร่วมกับหน้า Login แต่ในหน้าสมัครสมาชิกจะใช้ property เพิ่มเติม ได้แก่ `Email`, `Phone` และ `ConfirmPassword` เพื่อใช้ตรวจสอบความถูกต้องก่อนสร้างบัญชีใหม่

รูปที่ควรแปะ

- `Viewmodels/AuthUserViewModel.cs` บรรทัด 1-10

อธิบายใต้รูป

- หน้า Signup ใช้ `Username`, `Email`, `Phone`, `Password`, `ConfirmPassword`
- ViewModel ตัวเดียวช่วยให้ส่วนสมาชิกอยู่ในโครงสร้างเดียวกัน

### 2. Controller
การทำงานของหน้า Signup อยู่ใน `AccountController` action `Signup` ทั้งแบบ `GET` และ `POST`

รูปที่ควรแปะ

- `Controllers/AccountController.cs` บรรทัด 71-121
- ถ้าแคปไม่หมดในรูปเดียว ให้แบ่งเป็น 2 รูปต่อเนื่องกัน

ลำดับการทำงาน

1. ระบบเปิดหน้า Signup โดยเรียก `GET Signup()` เพื่อสร้าง `AuthUserViewModel` และส่งไปยังหน้า View
2. เมื่อผู้ใช้กรอกข้อมูลสมัครสมาชิกและกดส่งฟอร์ม ข้อมูลจะถูกส่งกลับมายัง `POST Signup(AuthUserViewModel model)`
3. Controller จะเตรียมข้อมูลเบื้องต้น เช่น ตัดช่องว่างของ `Username`, `Email` และ `Phone`
4. ระบบตรวจสอบก่อนว่ารหัสผ่านกับการยืนยันรหัสผ่านตรงกันหรือไม่
5. จากนั้นระบบจะตรวจสอบว่าผู้ใช้กรอกข้อมูลสำคัญครบหรือไม่ เช่น ชื่อผู้ใช้ อีเมล และรหัสผ่าน
6. หากข้อมูลครบ ระบบจะตรวจสอบต่อว่ามี `Username` หรือ `Email` ซ้ำในตาราง `Users` หรือไม่
7. ถ้าไม่ซ้ำ ระบบจะสร้างข้อมูลผู้ใช้ใหม่ โดยกำหนด `RoleId = 3` ให้เป็นผู้ใช้ทั่วไป
8. หลังบันทึกข้อมูลผู้ใช้ลงฐานข้อมูล ระบบจะเรียก `GrantWelcomePromotion(...)` เพื่อมอบโปรโมชันต้อนรับให้สมาชิกใหม่
9. เมื่อสมัครสมาชิกสำเร็จ ระบบจะ redirect ไปยังหน้า Login เพื่อให้ผู้ใช้เข้าสู่ระบบต่อ

### 3. View
หน้า Signup อยู่ใน `Views/Account/Signup.cshtml` และใช้ `AuthUserViewModel` สำหรับรับข้อมูลจากฟอร์มสมัครสมาชิก

รูปที่ควรแปะ

- `Views/Account/Signup.cshtml` บรรทัด 241-277

อธิบายใต้รูป

- หน้า View ใช้ `@model AuthUserViewModel`
- มีฟอร์มสมัครสมาชิกที่ประกอบด้วย `Username`, `Email`, `Phone`, `Password`, `ConfirmPassword`
- มีปุ่มสมัครสมาชิกและลิงก์กลับไปยังหน้า Login
- มีส่วนสำหรับแสดงข้อความผิดพลาดเมื่อกรอกข้อมูลไม่ถูกต้อง

### 4. ผลลัพธ์ที่ผู้ใช้เห็น

- ผู้ใช้จะเห็นฟอร์มสมัครสมาชิกที่กรอกชื่อ อีเมล เบอร์โทร และรหัสผ่าน
- ถ้ากรอกข้อมูลไม่ครบ รหัสผ่านไม่ตรงกัน หรือข้อมูลซ้ำ ระบบจะแสดงข้อความเตือน
- ถ้าสมัครสมาชิกสำเร็จ ระบบจะพาไปหน้า Login

### 5. ตารางฐานข้อมูลที่เกี่ยวข้อง

- `Users`
  ใช้บันทึกข้อมูลบัญชีผู้ใช้ใหม่
- `Roles`
  ใช้อ้างอิงบทบาทของผู้ใช้
- `UserPromotions`
  ใช้บันทึกโปรโมชันต้อนรับที่มอบให้สมาชิกใหม่
- `Promotions`
  ใช้อ้างอิงโปรโมชันต้อนรับที่นำไปมอบให้ผู้ใช้ใหม่

---

## 3. ระบบกู้คืนรหัสผ่าน

### 1. ViewModel ที่ใช้
ระบบกู้คืนรหัสผ่านใช้ ViewModel แยกตามแต่ละขั้นตอน ได้แก่ `ForgotPasswordRequestViewModel`, `ForgotPasswordVerifyOtpViewModel` และ `ForgotPasswordResetViewModel` เพื่อแยกหน้าที่ของการขอ OTP การยืนยัน OTP และการตั้งรหัสผ่านใหม่ให้ชัดเจน

รูปที่ควรแปะ

- `Models/ForgotPasswordViewModels.cs` บรรทัด 1-20

อธิบายใต้รูป

- `ForgotPasswordRequestViewModel` ใช้รับค่าอีเมลในขั้นตอนขอ OTP
- `ForgotPasswordVerifyOtpViewModel` ใช้รับค่าอีเมลและ OTP ในขั้นตอนยืนยันรหัส
- `ForgotPasswordResetViewModel` ใช้รับค่าอีเมล OTP รหัสผ่านใหม่ และยืนยันรหัสผ่าน

### 2. Controller
การทำงานของระบบกู้คืนรหัสผ่านอยู่ใน `AccountController` โดยแยกเป็น `ForgotPassword`, `RequestPasswordReset`, `VerifyPasswordResetOtp` และ `ResetPassword`

รูปที่ควรแปะ

- `Controllers/AccountController.cs` บรรทัด 131-246
- ถ้าแคปไม่หมดในรูปเดียว ให้แบ่งเป็นหลายรูปต่อเนื่องกัน แล้วอธิบายรวมตามลำดับการทำงาน

ลำดับการทำงาน

1. ระบบเปิดหน้า `ForgotPassword()` เพื่อแสดงหน้ารีเซ็ตรหัสผ่านให้ผู้ใช้กรอกข้อมูลในหน้าเดียว
2. เมื่อผู้ใช้กรอกอีเมลและกดขอรหัส ระบบจะเรียก `RequestPasswordReset(...)`
3. Controller ตรวจสอบก่อนว่าอีเมลถูกกรอกและมีอยู่จริงในตาราง `Users`
4. ถ้าพบผู้ใช้ ระบบจะสร้าง OTP แบบตัวเลข 4 หลัก พร้อมกำหนดเวลาหมดอายุ 5 นาที แล้วบันทึกลงในตาราง `Users`
5. หลังจากนั้นเมื่อผู้ใช้กรอก OTP ระบบจะเรียก `VerifyPasswordResetOtp(...)` เพื่อตรวจสอบว่าอีเมล รหัส OTP และเวลาหมดอายุยังถูกต้อง
6. ถ้า OTP ถูกต้อง ระบบจะบันทึกสถานะการยืนยันไว้ใน `Session` เพื่อใช้ในขั้นตอนตั้งรหัสผ่านใหม่
7. เมื่อผู้ใช้กรอกรหัสผ่านใหม่และยืนยันรหัสผ่าน ระบบจะเรียก `ResetPassword(...)`
8. Controller จะตรวจสอบอีกครั้งว่า session การยืนยัน OTP ยังถูกต้อง รหัสผ่านใหม่ตรงกัน และมีความยาวอย่างน้อย 6 ตัวอักษร
9. ถ้าทุกเงื่อนไขผ่าน ระบบจะอัปเดตรหัสผ่านใหม่ในตาราง `Users` แล้วล้างค่า OTP และ session ที่เกี่ยวข้องออก
10. สุดท้ายระบบจะส่งผลลัพธ์กลับไปให้หน้าเว็บ เพื่อแจ้งว่าการรีเซ็ตรหัสผ่านสำเร็จ

### 3. View
หน้า Forgot Password อยู่ในไฟล์ `Views/Account/ForgotPassword.cshtml` และเป็นหน้าที่รวมส่วนกรอกอีเมล กรอก OTP และตั้งรหัสผ่านใหม่ไว้ในหน้าเดียว

รูปที่ควรแปะ

- `Views/Account/ForgotPassword.cshtml` บรรทัด 543-595
- `Views/Account/ForgotPassword.cshtml` บรรทัด 557-582

อธิบายใต้รูป

- หน้า View มีส่วนกรอก `Email`, `OtpCode`, `NewPassword` และ `ConfirmPassword`
- หน้านี้แบ่งการทำงานเป็น 3 ช่วง คือ ขอ OTP, ยืนยัน OTP และตั้งรหัสผ่านใหม่
- มีปุ่มสำหรับดำเนินการแต่ละขั้นตอนต่อเนื่องภายในหน้าเดียว

### 4. ผลลัพธ์ที่ผู้ใช้เห็น

- ผู้ใช้จะเห็นหน้า Forgot Password ที่ให้กรอกอีเมล ขอ OTP และตั้งรหัสผ่านใหม่
- ถ้าอีเมลไม่ถูกต้อง หรือ OTP ไม่ถูกต้อง ระบบจะแสดงข้อความผิดพลาดบนหน้าเดิม
- ถ้าข้อมูลถูกต้อง ระบบจะอนุญาตให้ตั้งรหัสผ่านใหม่และแจ้งว่าดำเนินการสำเร็จ

### 5. ตารางฐานข้อมูลที่เกี่ยวข้อง

- `Users`
  ใช้ตรวจสอบอีเมล บันทึก `OtpCode` `OtpExpiredAt` และอัปเดตรหัสผ่านใหม่
- `Session`
  ใช้เก็บสถานะว่าผู้ใช้ผ่านการยืนยัน OTP แล้วก่อนตั้งรหัสผ่านใหม่

---

## 4. หน้า Home

### 4.1 ViewModel ที่ใช้
หน้า Home ของระบบนี้ไม่ได้ใช้ ViewModel แยกเฉพาะหน้า แต่ใช้ `ViewBag` ส่งข้อมูลโปรโมชันและสินค้าเด่นจาก Controller ไปยัง View โดยตรง

รูปที่ควรแปะ

- `Controllers/HomeController.cs` บรรทัด 18-27

อธิบายใต้รูป

- หน้า Home ใช้ `ViewBag.PublicPromotions` สำหรับส่งรายการโปรโมชันสาธารณะ
- ใช้ `ViewBag.FeaturedProducts` สำหรับส่งรายการสินค้าเด่นไปแสดงบนหน้าแรก

### 4.2 Controller
การทำงานของหน้า Home อยู่ใน `HomeController.Home()`

รูปที่ควรแปะ

- `Controllers/HomeController.cs` บรรทัด 18-27

ลำดับการทำงาน

1. ระบบเปิดหน้า `Home()` เพื่อแสดงหน้าแรกของเว็บไซต์
2. Controller ดึงข้อมูลโปรโมชันที่เปิดใช้งานจากตาราง `Promotions`
3. จากนั้นระบบดึงสินค้าที่ต้องการแสดงเป็นสินค้าเด่นจากตาราง `Stocks`
4. Controller นำข้อมูลทั้งหมดไปเก็บไว้ใน `ViewBag`
5. สุดท้ายระบบส่งข้อมูลไปยังหน้า View เพื่อแสดงเนื้อหาบนหน้าแรก

### 4.3 View
หน้า Home อยู่ใน `Views/Account/Home.cshtml`

รูปที่ควรแปะ

- `Views/Account/Home.cshtml` บรรทัด 1-24
- `Views/Account/Home.cshtml` บรรทัด 1590-1620

อธิบายใต้รูป

- หน้า View รับข้อมูลโปรโมชันและสินค้าเด่นจาก `ViewBag`
- มีการวนแสดงข้อมูลด้วย `foreach` เพื่อสร้าง card หรือ section ของหน้าแรก

### 4.4 ผลลัพธ์ที่ผู้ใช้เห็น

- ผู้ใช้จะเห็นหน้าแรกของร้าน พร้อมแบนเนอร์ ข้อมูลแนะนำ และรายการโปรโมชัน
- ผู้ใช้สามารถเห็นสินค้าเด่นหรือสินค้าที่แนะนำบนหน้าแรกได้ทันที

### 4.5 ตารางฐานข้อมูลที่เกี่ยวข้อง

- `Promotions`
  ใช้ดึงข้อมูลโปรโมชันที่ต้องการแสดงบนหน้าแรก
- `Stocks`
  ใช้ดึงข้อมูลสินค้าเด่นและสินค้าที่พร้อมแสดง

---

## 5. หน้า Menu

### 5.1 ViewModel ที่ใช้
หน้า Menu ไม่ได้ใช้ ViewModel แยก แต่ Controller ส่งข้อมูลแบบ `List<Stock>` ไปยัง View โดยตรง

รูปที่ควรแปะ

- `Controllers/HomeController.cs` บรรทัด 30-39

อธิบายใต้รูป

- หน้า Menu ใช้รายการสินค้าโดยตรงจากตาราง `Stocks`
- มีการ include ข้อมูลหมวดหมู่จาก `Categories` เพื่อแสดงชื่อหมวดหมู่ของสินค้า

### 5.2 Controller
การทำงานของหน้า Menu อยู่ใน `HomeController.Menu()`

รูปที่ควรแปะ

- `Controllers/HomeController.cs` บรรทัด 30-39

ลำดับการทำงาน

1. ระบบเปิดหน้า `Menu()` เพื่อแสดงรายการสินค้าทั้งหมด
2. Controller ดึงข้อมูลสินค้าจากตาราง `Stocks`
3. ระบบกรองเฉพาะสินค้าที่พร้อมขายด้วยเงื่อนไข `IsAvailable`
4. จากนั้น Controller ดึงข้อมูลหมวดหมู่ของสินค้าจาก `Categories` มาร่วมแสดง
5. สุดท้ายระบบส่งรายการสินค้าทั้งหมดไปยังหน้า View

### 5.3 View
หน้า Menu อยู่ใน `Views/Account/Menu.cshtml`

รูปที่ควรแปะ

- `Views/Account/Menu.cshtml` บรรทัด 1-6
- `Views/Account/Menu.cshtml` บรรทัด 789-1110
- `Views/Account/Menu.cshtml` บรรทัด 1147-1165

อธิบายใต้รูป

- หน้า View ใช้ `@model List<Stock>`
- มีการวนแสดงรายการสินค้าทั้งหมดผ่าน `foreach`
- แต่ละรายการแสดงชื่อสินค้า ราคา รูปภาพ และจำนวนคงเหลือ

### 5.4 ผลลัพธ์ที่ผู้ใช้เห็น

- ผู้ใช้จะเห็นรายการสินค้าทั้งหมดที่พร้อมขายในระบบ
- ผู้ใช้สามารถดูราคา รายละเอียด และจำนวนคงเหลือของสินค้าแต่ละรายการได้

### 5.5 ตารางฐานข้อมูลที่เกี่ยวข้อง

- `Stocks`
  ใช้เก็บข้อมูลสินค้า ราคา รูปภาพ และสถานะพร้อมขาย
- `Categories`
  ใช้เก็บชื่อหมวดหมู่ของสินค้า

---

## 6. หน้า Promotion

### 6.1 ViewModel ที่ใช้
หน้า Promotion ใช้ `UserPromotionPageViewModel` เพื่อรวมข้อมูลโปรโมชันทั้งหมด โปรโมชันกิจกรรม และข้อมูลรางวัลที่ต้องแสดงบนหน้าเดียวกัน

รูปที่ควรแปะ

- `Viewmodels/UserPromotionPageViewModel.cs` บรรทัด 1-10

อธิบายใต้รูป

- ViewModel ตัวนี้ใช้รวบรวมข้อมูลโปรโมชันหลายประเภทให้อยู่ในโครงสร้างเดียว
- ช่วยให้หน้า Promotion แสดงข้อมูลโปรโมชันและส่วนขอรับโปรโมชันได้ในหน้าเดียว

### 6.2 Controller
การทำงานของหน้า Promotion อยู่ใน `HomeController.Promotion()`

รูปที่ควรแปะ

- `Controllers/HomeController.cs` บรรทัด 42-63

ลำดับการทำงาน

1. ระบบเปิดหน้า `Promotion()` เพื่อแสดงรายการโปรโมชันของร้าน
2. Controller ดึงข้อมูลโปรโมชันที่เปิดใช้งานจากตาราง `Promotions`
3. ระบบจัดรูปข้อมูลให้อยู่ใน `UserPromotionPageViewModel`
4. ถ้ามีโปรโมชันกิจกรรมที่เกี่ยวข้องกับของรางวัล ระบบจะค้นหาข้อมูลชื่อสินค้าหรือรางวัลจาก `Stocks` เพิ่มเติม
5. สุดท้ายระบบส่งข้อมูลทั้งหมดไปยังหน้า View เพื่อแสดงผล

### 6.3 View
หน้า Promotion อยู่ใน `Views/Account/Promotion.cshtml`

รูปที่ควรแปะ

- `Views/Account/Promotion.cshtml` บรรทัด 1-24
- `Views/Account/Promotion.cshtml` บรรทัด 615-718
- `Views/Account/Promotion.cshtml` บรรทัด 669-718

อธิบายใต้รูป

- หน้า View ใช้ `@model UserPromotionPageViewModel`
- มีส่วนแสดงรายการโปรโมชันที่เปิดใช้งาน
- มีส่วนรับข้อมูลหรือแนบหลักฐานสำหรับโปรโมชันกิจกรรม

### 6.4 ผลลัพธ์ที่ผู้ใช้เห็น

- ผู้ใช้สามารถดูรายการโปรโมชันของระบบได้
- ผู้ใช้สามารถดูรายละเอียดของรางวัลหรือกิจกรรมที่เกี่ยวข้องกับโปรโมชันได้

### 6.5 ตารางฐานข้อมูลที่เกี่ยวข้อง

- `Promotions`
  ใช้เก็บข้อมูลโปรโมชันหลักของระบบ
- `Stocks`
  ใช้ดึงข้อมูลสินค้าหรือรางวัลที่เกี่ยวข้องกับโปรโมชัน

---

## 7. ระบบจัดการโปรไฟล์

### 7.1 ViewModel ที่ใช้
ระบบจัดการโปรไฟล์ใช้ `ProfileViewModel` เป็นตัวกลางระหว่าง `ProfileController` กับหน้า View

รูปที่ควรแปะ

- `Viewmodels/ProfileViewModel.cs` บรรทัด 1-19

อธิบายใต้รูป

- `ProfileViewModel` เก็บข้อมูลผู้ใช้ เช่น `UserId`, `Username`, `Email`, `Phone`
- ใน ViewModel เดียวกันยังรองรับการเปลี่ยนรหัสผ่านด้วย `CurrentPassword`, `NewPassword`, `ConfirmPassword`
- และมี `Addresses` เพื่อใช้แสดงรายการที่อยู่ของผู้ใช้ในหน้าเดียวกัน

### 7.2 Controller
การทำงานของระบบโปรไฟล์อยู่ใน `ProfileController` action `Profile` ทั้งแบบ `GET` และ `POST`

รูปที่ควรแปะ

- `Controllers/ProfileController.cs` บรรทัด 16-80

ลำดับการทำงาน

1. ระบบเปิดหน้าโปรไฟล์โดยเรียก `GET Profile(int id = 0)` เพื่อโหลดข้อมูลผู้ใช้ปัจจุบัน
2. ถ้าไม่ได้ส่ง `id` เข้ามา ระบบจะอ่าน `UserId` จาก `Session`
3. ถ้าไม่พบผู้ใช้หรือยังไม่ได้เข้าสู่ระบบ ระบบจะ redirect กลับไปหน้า Login
4. เมื่อพบผู้ใช้แล้ว ระบบจะดึงข้อมูลบัญชีและรายการที่อยู่จากฐานข้อมูล แล้วรวมเป็น `ProfileViewModel`
5. เมื่อผู้ใช้แก้ไขข้อมูลโปรไฟล์ หน้าเว็บจะส่งข้อมูลกลับมายัง `POST Profile([FromBody] ProfileViewModel model)`
6. Controller จะตรวจสอบว่า `UserId` ถูกต้องและมีผู้ใช้อยู่จริง
7. จากนั้นระบบจะอัปเดตข้อมูลพื้นฐาน เช่น `Username`, `Email` และ `Phone`
8. ถ้าผู้ใช้ต้องการเปลี่ยนรหัสผ่าน ระบบจะตรวจสอบรหัสผ่านปัจจุบัน ตรวจสอบความตรงกันของรหัสผ่านใหม่ และตรวจสอบความยาวขั้นต่ำก่อนบันทึก
9. เมื่อบันทึกสำเร็จ ระบบจะส่งผลลัพธ์กลับเป็น JSON ให้หน้าเว็บแสดงข้อความตอบกลับ

### 7.3 View
หน้า Profile อยู่ใน `Views/Account/Profile.cshtml`

รูปที่ควรแปะ

- `Views/Account/Profile.cshtml` บรรทัด 1-25
- `Views/Account/Profile.cshtml` บรรทัด 1-2
- `Views/Account/Profile.cshtml` บรรทัด 515-540
- `Views/Account/Profile.cshtml` บรรทัด 551-589

อธิบายใต้รูป

- หน้า View ใช้ `@model ProfileViewModel`
- มีฟอร์มแสดงข้อมูล `UserId`, `Username`, `Email` และ `Phone` จาก `Model`
- ใช้ `@foreach` แสดงรายการที่อยู่จาก `Model.Addresses`

### 7.4 ผลลัพธ์ที่ผู้ใช้เห็น

- ผู้ใช้จะเห็นข้อมูลบัญชีและรายการที่อยู่ของตนเองในหน้าเดียวกัน
- ผู้ใช้สามารถแก้ไขข้อมูลพื้นฐานและเปลี่ยนรหัสผ่านได้
- เมื่อบันทึกสำเร็จ ระบบจะแจ้งข้อความผลลัพธ์กลับไปบนหน้าเว็บ

### 7.5 ตารางฐานข้อมูลที่เกี่ยวข้อง

- `Users`
  ใช้ดึงและอัปเดตข้อมูลผู้ใช้
- `Addresses`
  ใช้แสดงรายการที่อยู่ของผู้ใช้ในหน้าโปรไฟล์

---

## 8. ระบบจัดการที่อยู่

### 8.1 ViewModel ที่ใช้
ระบบจัดการที่อยู่ไม่ได้ใช้ ViewModel แยกเฉพาะ แต่ใช้ `ProfileViewModel` ร่วมกับหน้า Profile และใช้ parameter binding ใน action ที่เกี่ยวข้อง

รูปที่ควรแปะ

- `Viewmodels/ProfileViewModel.cs` บรรทัด 1-19

อธิบายใต้รูป

- ที่อยู่ถูกจัดการผ่านหน้า Profile และหน้า Checkout
- ในส่วนบันทึกและลบที่อยู่ ระบบรับค่าผ่าน parameter โดยตรง

### 8.2 Controller
การทำงานของระบบจัดการที่อยู่ อยู่ใน `ProfileController` ได้แก่ `SaveAddress(...)`, `DeleteAddress(...)` และ `GetUserAddresses(...)`

รูปที่ควรแปะ

- `Controllers/ProfileController.cs` บรรทัด 84-133

ลำดับการทำงาน

1. เมื่อผู้ใช้เพิ่มหรือแก้ไขที่อยู่ หน้าเว็บจะเรียก `SaveAddress(...)`
2. ถ้า `addressId > 0` ระบบจะค้นหาที่อยู่เดิม แล้วอัปเดตข้อมูลใหม่
3. ถ้าเป็นการเพิ่มใหม่ ระบบจะสร้างข้อมูลที่อยู่ในตาราง `Addresses`
4. `DeleteAddress(...)` ใช้ลบที่อยู่ของผู้ใช้ตาม `addressId`
5. `GetUserAddresses(...)` ใช้ดึงรายการที่อยู่ทั้งหมดของผู้ใช้กลับไปในรูปแบบ JSON
6. หน้า Checkout และหน้า Profile จะเรียก endpoint นี้เพื่อนำที่อยู่ไปแสดงผล

### 8.3 View
ส่วนที่เกี่ยวข้องกับที่อยู่ อยู่ใน `Views/Account/Profile.cshtml` และ `Views/Account/Checkout.cshtml`

รูปที่ควรแปะ

- `Views/Account/Profile.cshtml` บรรทัด 551-589
- `Views/Account/Profile.cshtml` บรรทัด 605-631
- `Views/Account/Checkout.cshtml` บรรทัด 21-33

อธิบายใต้รูป

- หน้า Profile มีส่วนแสดงรายการที่อยู่เดิมและฟอร์มเพิ่มหรือแก้ไขที่อยู่
- หน้า Checkout มีส่วนสำหรับเลือกที่อยู่จัดส่งของผู้ใช้
- ผู้ใช้สามารถมองเห็นรายการที่อยู่ที่มีอยู่และเลือกใช้งานได้

### 8.4 ผลลัพธ์ที่ผู้ใช้เห็น

- ผู้ใช้สามารถเพิ่ม แก้ไข และลบที่อยู่ได้
- หน้าโปรไฟล์จะแสดงรายการที่อยู่ปัจจุบันของผู้ใช้
- หน้า Checkout สามารถเรียกที่อยู่ไปใช้งานต่อได้

### 8.5 ตารางฐานข้อมูลที่เกี่ยวข้อง

- `Addresses`
  ใช้บันทึก ลบ และดึงข้อมูลที่อยู่ของผู้ใช้
- `Users`
  ใช้ตรวจสอบเจ้าของข้อมูลจาก `UserId`

---

## 9. ระบบสั่งซื้อสินค้า

### 9.1 ViewModel ที่ใช้
ระบบสั่งซื้อสินค้าใช้ `OrderRequest` จาก `Viewmodels/OrderViewModel.cs` เป็นตัวรับข้อมูลคำสั่งซื้อจากหน้า Checkout

รูปที่ควรแปะ

- `Viewmodels/OrderViewModel.cs` บรรทัด 1-20

อธิบายใต้รูป

- `OrderRequest` เก็บข้อมูล `AddressId`, รายการสินค้า และโปรโมชันที่ใช้
- ใช้เป็นตัวกลางระหว่างหน้า Checkout กับ `OrderController.CreateOrder(...)`

### 9.2 Controller
การทำงานของระบบสั่งซื้อสินค้าอยู่ใน `OrderController` ได้แก่ `Checkout()`, `CreateOrder(...)` และ `GetCurrentUser()`

รูปที่ควรแปะ

- `Controllers/OrderController.cs` บรรทัด 27-168

ลำดับการทำงาน

1. ผู้ใช้เปิดหน้า `Checkout()` เพื่อเข้าสู่ขั้นตอนสั่งซื้อ
2. หน้าเว็บจะเรียก `GetCurrentUser()` เพื่อดึงข้อมูลผู้ใช้ปัจจุบัน
3. จากนั้นเมื่อผู้ใช้กดยืนยันคำสั่งซื้อ ข้อมูลจะถูกส่งมายัง `CreateOrder([FromBody] OrderRequest model)`
4. ระบบตรวจสอบก่อนว่ามีสินค้าในคำสั่งซื้อจริง และผู้ใช้มีสิทธิ์ใช้งานในระบบ
5. ถ้ามีการเลือกโปรโมชัน ระบบจะตรวจสอบว่ากิจกรรมหรือโปรโมชันนั้นยังใช้งานได้อยู่
6. จากนั้นระบบจะสร้างข้อมูลหัวออเดอร์ในตาราง `Orders`
7. ระบบวนลูปรายการสินค้าแต่ละชิ้น แล้วบันทึกลงในตาราง `Orderdetails`
8. ถ้ามีโปรโมชันแบบของแถม ระบบจะสร้างรายการของแถมลงใน `Orderdetails` เพิ่มเติม
9. ถ้าใช้โปรโมชันของผู้ใช้ ระบบจะอัปเดต `UserPromotions` ให้เป็นสถานะใช้งานแล้ว
10. หลังสร้างออเดอร์สำเร็จ ระบบจะสร้างการแจ้งเตือนไปยังผู้ใช้ และส่ง `orderId` กลับไปให้หน้าเว็บ

### 9.3 View
หน้า Checkout อยู่ใน `Views/Account/Checkout.cshtml`

รูปที่ควรแปะ

- `Views/Account/Checkout.cshtml` บรรทัด 21-35
- `Views/Account/Checkout.cshtml` บรรทัด 45-69

อธิบายใต้รูป

- หน้า View มีส่วนเลือกที่อยู่ เลือกโปรโมชัน และสรุปยอดสั่งซื้อ
- ผู้ใช้สามารถตรวจสอบข้อมูลก่อนกดยืนยันคำสั่งซื้อได้จากหน้าเดียว

### 9.4 ผลลัพธ์ที่ผู้ใช้เห็น

- ผู้ใช้สามารถเลือกที่อยู่และโปรโมชันก่อนสร้างคำสั่งซื้อ
- เมื่อสั่งซื้อสำเร็จ ระบบจะส่งผู้ใช้ไปยังหน้า Payment
- ถ้าข้อมูลไม่ครบหรือเงื่อนไขไม่ผ่าน ระบบจะแจ้งข้อความกลับไปบนหน้าเว็บ

### 9.5 ตารางฐานข้อมูลที่เกี่ยวข้อง

- `Orders`
  ใช้บันทึกข้อมูลหัวออเดอร์
- `Orderdetails`
  ใช้บันทึกรายการสินค้าแต่ละชิ้นในคำสั่งซื้อ
- `UserPromotions`
  ใช้อัปเดตสถานะสิทธิ์โปรโมชันของผู้ใช้
- `Promotions`
  ใช้ตรวจสอบข้อมูลโปรโมชันที่เลือก
- `Addresses`
  ใช้ตรวจสอบที่อยู่จัดส่ง
- `Notifications`
  ใช้สร้างข้อความแจ้งเตือนหลังสร้างคำสั่งซื้อ

---

## 10. ระบบใช้งานโปรโมชันและขอรับโปรโมชัน

### 10.1 ViewModel ที่ใช้
ระบบโปรโมชันใช้ `UserPromotionPageViewModel` สำหรับหน้า Promotion และใช้การส่งค่าผ่าน parameter ในการขอรับโปรโมชันกิจกรรม

รูปที่ควรแปะ

- `Viewmodels/UserPromotionPageViewModel.cs` บรรทัด 1-10

อธิบายใต้รูป

- หน้า Promotion ใช้ `UserPromotionPageViewModel` เพื่อรวมข้อมูลโปรโมชันปกติ สิทธิ์โปรโมชันของผู้ใช้ และข้อมูลโปรโมชันกิจกรรม
- การขอรับโปรโมชันกิจกรรมใช้ `promotionId`, `proofImage` และ `note` เป็นตัวรับข้อมูลหลัก

### 10.2 Controller
การทำงานของระบบโปรโมชันอยู่ใน `OrderController.GetUserPromos(...)` และ `OrderController.SubmitPromotionClaim(...)`

รูปที่ควรแปะ

- `Controllers/OrderController.cs` บรรทัด 172-275

ลำดับการทำงาน

1. เมื่อหน้า Checkout ต้องการใช้โปรโมชัน ระบบจะเรียก `GetUserPromos(int userId)`
2. Controller ตรวจสอบก่อนว่าผู้ใช้ที่ร้องขอเป็นเจ้าของข้อมูลจริง
3. ถ้าถูกต้อง ระบบจะดึงข้อมูลสิทธิ์โปรโมชันของผู้ใช้จาก `UserPromotions`
4. ระบบจะค้นหาสิทธิ์โปรโมชันที่ยังไม่ถูกใช้จาก `UserPromotions` และรวมข้อมูลจาก `Promotions`
5. ถ้าผู้ใช้ต้องการขอรับโปรโมชันกิจกรรม ระบบจะส่งฟอร์มไปยัง `SubmitPromotionClaim(...)`
6. Controller จะตรวจสอบว่าโปรโมชันยังใช้งานได้ รับไฟล์รูปที่ถูกประเภท ขนาดไม่เกินกำหนด และยังไม่มีคำขอค้างอยู่
7. ถ้าผ่านเงื่อนไข ระบบจะบันทึกไฟล์หลักฐานลงในโฟลเดอร์ `wwwroot/uploads/promotion-claims`
8. จากนั้นระบบจะสร้างข้อมูลคำขอใหม่ในตาราง `PromotionClaims` และแจ้งเตือนไปยังผู้ใช้

### 10.3 View
ส่วนที่เกี่ยวข้องกับโปรโมชัน อยู่ใน `Views/Account/Promotion.cshtml` และ `Views/Account/Checkout.cshtml`

รูปที่ควรแปะ

- `Views/Account/Promotion.cshtml` บรรทัด 1-4
- `Views/Account/Promotion.cshtml` บรรทัด 615-660
- `Views/Account/Promotion.cshtml` บรรทัด 684-718
- `Views/Account/Checkout.cshtml` บรรทัด 45-49

อธิบายใต้รูป

- หน้า Promotion ใช้ `@model UserPromotionPageViewModel`
- มีส่วนแสดงรายการโปรโมชันด้วย `@foreach`
- มีฟอร์มขอรับโปรโมชันกิจกรรมจากข้อมูลในหน้าเดียวกัน
- หน้า Checkout มีส่วนดึงสิทธิ์โปรโมชันของผู้ใช้ไปใช้งาน

### 10.4 ผลลัพธ์ที่ผู้ใช้เห็น

- ผู้ใช้สามารถดูโปรโมชันของระบบได้
- ผู้ใช้สามารถส่งคำขอรับโปรโมชันกิจกรรมพร้อมแนบหลักฐานได้
- ในหน้า Checkout ผู้ใช้สามารถเลือกสิทธิ์โปรโมชันที่ยังไม่ถูกใช้มาใช้งานกับคำสั่งซื้อได้

### 10.5 ตารางฐานข้อมูลที่เกี่ยวข้อง

- `Promotions`
  ใช้เก็บข้อมูลโปรโมชันหลักของระบบ
- `UserPromotions`
  ใช้เก็บสิทธิ์โปรโมชันของผู้ใช้
- `PromotionClaims`
  ใช้เก็บคำขอรับโปรโมชันกิจกรรม
- `Notifications`
  ใช้สร้างข้อความแจ้งเตือนหลังส่งคำขอสำเร็จ
- `wwwroot/uploads/promotion-claims`
  ใช้เก็บไฟล์หลักฐานการเข้าร่วมกิจกรรม

---

## 11. ระบบชำระเงิน

### 11.1 ViewModel ที่ใช้
หน้า Payment ไม่ได้ใช้ ViewModel แยกเฉพาะหน้า แต่ Controller ส่ง `Order` ไปยัง View โดยตรง และใช้ `ViewBag` ช่วยส่งข้อมูลยอดเงินกับ `OrderId`

รูปที่ควรแปะ

- `Views/Account/Payment.cshtml` บรรทัด 1-2
- `Views/Account/Payment.cshtml` บรรทัด 176-200

อธิบายใต้รูป

- หน้า Payment ใช้ `@model Order`
- `ViewBag.TotalAmount` และ `ViewBag.OrderId` ใช้แสดงข้อมูลที่จำเป็นสำหรับการชำระเงิน

### 11.2 Controller
การทำงานของระบบชำระเงินอยู่ใน `OrderController.Payment`

รูปที่ควรแปะ

- `Controllers/OrderController.cs` บรรทัด 277-303

ลำดับการทำงาน

1. ผู้ใช้เข้าสู่หน้า `Payment(int orderId)` หลังจากสร้างออเดอร์สำเร็จ
2. Controller ตรวจสอบก่อนว่าผู้ใช้เข้าสู่ระบบอยู่จริง
3. จากนั้นระบบจะค้นหาออเดอร์ของผู้ใช้จากตาราง `Orders` พร้อมข้อมูลรายการสินค้าและโปรโมชัน
4. ถ้าไม่พบออเดอร์ ระบบจะ redirect กลับไปหน้า Home
5. เมื่อพบออเดอร์แล้ว ระบบจะใช้ `PromptPayHelper.GeneratePayload(...)` สร้างข้อมูล payload สำหรับ PromptPay
6. จากนั้นระบบจะสร้าง QR Code ด้วย `QRCoder`
7. Controller ส่งข้อมูล QR Code, `OrderId` และยอดเงินรวมไปยังหน้า View เพื่อให้ผู้ใช้ชำระเงิน

### 11.3 View
หน้า Payment อยู่ใน `Views/Account/Payment.cshtml`

รูปที่ควรแปะ

- `Views/Account/Payment.cshtml` บรรทัด 176-200

อธิบายใต้รูป

- หน้า View แสดงยอดเงิน `OrderId` และ QR Code ที่ใช้ชำระเงิน
- มีช่องเลือกไฟล์สลิปและปุ่มยืนยันการชำระเงินในหน้าเดียวกัน

### 11.4 ผลลัพธ์ที่ผู้ใช้เห็น

- ผู้ใช้จะเห็นหน้า Payment พร้อมยอดชำระเงินและ QR Code
- ผู้ใช้สามารถใช้ข้อมูลนี้ไปชำระเงินก่อนอัปโหลดสลิป
- หลังจากนั้นจึงเข้าสู่ขั้นตอนยืนยันการชำระเงินผ่านการแนบหลักฐาน

### 11.5 ตารางฐานข้อมูลที่เกี่ยวข้อง

- `Orders`
  ใช้ตรวจสอบออเดอร์และยอดชำระเงิน
- `Orderdetails`
  ใช้แสดงรายการสินค้าในคำสั่งซื้อ
- `Promotions`
  ใช้แสดงส่วนลดหรือข้อมูลโปรโมชันที่เกี่ยวข้อง

---

## 12. ระบบอัปโหลดสลิป

### 12.1 ViewModel ที่ใช้
ระบบอัปโหลดสลิปไม่ได้ใช้ ViewModel แยก แต่รับข้อมูลผ่าน parameter โดยตรง คือ `orderId` และ `IFormFile slipImage`

รูปที่ควรแปะ

- `Controllers/OrderController.cs` บรรทัด 307-347

อธิบายใต้รูป

- ระบบใช้ `orderId` เพื่ออ้างอิงว่ากำลังอัปโหลดสลิปให้คำสั่งซื้อใด
- ใช้ `slipImage` รับไฟล์หลักฐานการชำระเงินจากผู้ใช้

### 12.2 Controller
การอัปโหลดสลิปอยู่ใน `OrderController.UploadSlip`

รูปที่ควรแปะ

- `Controllers/OrderController.cs` บรรทัด 307-347

ลำดับการทำงาน

1. หลังจากผู้ใช้ชำระเงินแล้ว หน้า Payment จะส่งไฟล์สลิปไปยัง `UploadSlip(int orderId, IFormFile slipImage)`
2. Controller ตรวจสอบก่อนว่าผู้ใช้ login อยู่จริงและออเดอร์นั้นเป็นของผู้ใช้คนนั้น
3. ระบบตรวจสอบว่าแนบไฟล์เข้ามาจริงหรือไม่
4. จากนั้นระบบจะสร้างชื่อไฟล์ใหม่ แล้วบันทึกไฟล์ไว้ในโฟลเดอร์ `wwwroot/uploads/slips`
5. เมื่อบันทึกไฟล์สำเร็จ ระบบจะอัปเดต `SlipImagePath` และ `PaymentStatus = "PendingVerify"` ในตาราง `Orders`
6. หลังจากนั้นระบบจะสร้าง `Notification` เพื่อแจ้งว่าระบบได้รับสลิปแล้วและกำลังรอการตรวจสอบ
7. สุดท้ายระบบจะส่งผลลัพธ์กลับไปยังหน้าเว็บ เพื่อให้ redirect ไปยังหน้า Delivery

### 12.3 View
ส่วนอัปโหลดสลิปอยู่ใน `Views/Account/Payment.cshtml`

รูปที่ควรแปะ

- `Views/Account/Payment.cshtml` บรรทัด 190-200

อธิบายใต้รูป

- หน้า View มีช่องเลือกไฟล์สลิปและปุ่มยืนยันการอัปโหลด
- ส่วนนี้เป็นหน้าสำหรับแนบหลักฐานการชำระเงินหลังโอนเงินสำเร็จ

### 12.4 ผลลัพธ์ที่ผู้ใช้เห็น

- ผู้ใช้สามารถเลือกไฟล์สลิปและกดยืนยันการอัปโหลดได้
- ถ้าอัปโหลดสำเร็จ ระบบจะแสดงข้อความว่ารอสถานะการตรวจสอบจาก Admin
- จากนั้นผู้ใช้จะถูกพาไปยังหน้าติดตามสถานะคำสั่งซื้อ

### 12.5 ตารางฐานข้อมูลที่เกี่ยวข้อง

- `Orders`
  ใช้บันทึก path ของสลิปและสถานะการชำระเงิน
- `Notifications`
  ใช้สร้างการแจ้งเตือนหลังอัปโหลดสลิปสำเร็จ
- `wwwroot/uploads/slips`
  ใช้เป็นที่เก็บไฟล์สลิปการชำระเงินจริง

---

## 13. ระบบติดตามสถานะคำสั่งซื้อ

### 13.1 ViewModel ที่ใช้
ระบบติดตามสถานะคำสั่งซื้อใช้ `DeliveryTrackingViewModel` เป็นตัวกลางสำหรับรวมข้อมูลออเดอร์ปัจจุบัน สถานะการติดตาม และประวัติคำสั่งซื้อของผู้ใช้

รูปที่ควรแปะ

- `Viewmodels/DeliveryTrackingViewModel.cs` บรรทัด 1-48

อธิบายใต้รูป

- `DeliveryTrackingViewModel` ใช้เก็บข้อมูลออเดอร์ปัจจุบัน เช่น `OrderNumber`, `OrderStatus`, `PaymentStatus`, `TrackingStage`
- ใน ViewModel เดียวกันมี `OrderHistory` สำหรับใช้แสดงประวัติคำสั่งซื้อย้อนหลัง
- มี class ย่อยสำหรับเก็บรายละเอียดรายการสินค้าในประวัติการสั่งซื้อ

### 13.2 Controller
การทำงานของระบบติดตามสถานะคำสั่งซื้ออยู่ใน `DeliveryController.Delivery()`

รูปที่ควรแปะ

- `Controllers/DeliveryController.cs` บรรทัด 20-67
- `Controllers/DeliveryController.cs` บรรทัด 132-277

ลำดับการทำงาน

1. ผู้ใช้เปิดหน้า `Delivery()` เพื่อดูสถานะออเดอร์และประวัติการสั่งซื้อ
2. Controller ตรวจสอบก่อนว่ามี `UserId` ใน `Session` หรือไม่
3. ถ้าพบผู้ใช้ ระบบจะค้นหาออเดอร์ปัจจุบันที่ยังไม่เสร็จสิ้นจากตาราง `Orders`
4. จากนั้นระบบจะดึงรายการออเดอร์ที่เสร็จสิ้นแล้วและข้อมูลจาก `Historyorders`
5. Controller จะจัดรูปข้อมูลทั้งหมดให้อยู่ใน `DeliveryTrackingViewModel`
6. สุดท้ายระบบส่ง ViewModel ไปยังหน้า View เพื่อแสดงสถานะล่าสุดและประวัติย้อนหลัง

### 13.3 View
หน้า Delivery อยู่ใน `Views/Account/Delivery.cshtml`

รูปที่ควรแปะ

- `Views/Account/Delivery.cshtml` บรรทัด 1-2
- `Views/Account/Delivery.cshtml` บรรทัด 854-941
- `Views/Account/Delivery.cshtml` บรรทัด 965-1033

อธิบายใต้รูป

- หน้า View ใช้ `@model DeliveryTrackingViewModel`
- มีส่วนแสดงสถานะออเดอร์ปัจจุบันจาก `@Model.OrderNumber`, `@Model.StatusTitle`, `@Model.PaymentStatus`, `@Model.DeliveryAddress`
- ใช้ `@for` แสดงประวัติคำสั่งซื้อจาก `Model.OrderHistory`

### 13.4 ผลลัพธ์ที่ผู้ใช้เห็น

- ผู้ใช้สามารถเห็นสถานะออเดอร์ปัจจุบันของตนเองได้
- ผู้ใช้สามารถตรวจสอบข้อมูลการชำระเงิน รายการสินค้า และที่อยู่จัดส่งได้
- ผู้ใช้สามารถดูประวัติคำสั่งซื้อย้อนหลังในหน้าเดียวกัน

### 13.5 ตารางฐานข้อมูลที่เกี่ยวข้อง

- `Orders`
  ใช้ดึงข้อมูลออเดอร์ปัจจุบันและสถานะการจัดส่ง
- `Orderdetails`
  ใช้ดึงรายการสินค้าในแต่ละคำสั่งซื้อ
- `Addresses`
  ใช้แสดงที่อยู่จัดส่งของคำสั่งซื้อ
- `Promotions`
  ใช้แสดงข้อมูลส่วนลดหรือโปรโมชันที่ใช้กับออเดอร์
- `Historyorders`
  ใช้เก็บประวัติคำสั่งซื้อที่เสร็จสิ้นแล้ว

---

## 14. ระบบยืนยันได้รับสินค้า

### 14.1 ViewModel ที่ใช้
ระบบยืนยันได้รับสินค้าไม่ได้ใช้ ViewModel แยกเฉพาะสำหรับ action นี้ แต่ทำงานร่วมกับ `DeliveryTrackingViewModel` ในหน้า Delivery และรับ `orderId` ผ่าน parameter โดยตรง

รูปที่ควรแปะ

- `Viewmodels/DeliveryTrackingViewModel.cs` บรรทัด 1-23
- `Controllers/DeliveryController.cs` บรรทัด 69-130

อธิบายใต้รูป

- หน้า Delivery ใช้ข้อมูลจาก `DeliveryTrackingViewModel` เพื่อแสดงปุ่มยืนยันรับสินค้า
- การยืนยันจะส่ง `orderId` ของออเดอร์ที่อยู่ในสถานะจัดส่ง

### 14.2 Controller
การยืนยันได้รับสินค้าอยู่ใน `DeliveryController.CompleteOrder(int orderId)`

รูปที่ควรแปะ

- `Controllers/DeliveryController.cs` บรรทัด 69-130

ลำดับการทำงาน

1. เมื่อผู้ใช้กดปุ่มยืนยันรับสินค้า ระบบจะเรียก `CompleteOrder(int orderId)`
2. Controller ตรวจสอบก่อนว่าผู้ใช้เข้าสู่ระบบอยู่จริง
3. จากนั้นระบบจะค้นหาออเดอร์ของผู้ใช้จากตาราง `Orders`
4. ระบบตรวจสอบว่าออเดอร์นั้นอยู่ในสถานะ `Shipped` ก่อนจึงจะยืนยันได้
5. ถ้ายังไม่มีประวัติ ระบบจะสร้างข้อมูลใหม่ในตาราง `Historyorders`
6. จากนั้นระบบจะอัปเดตสถานะของออเดอร์เป็น `Completed`
7. สุดท้ายระบบส่งผลลัพธ์กลับเป็น JSON เพื่อแจ้งว่าปิดออเดอร์สำเร็จ

### 14.3 View
ส่วนยืนยันได้รับสินค้าอยู่ใน `Views/Account/Delivery.cshtml`

รูปที่ควรแปะ

- `Views/Account/Delivery.cshtml` บรรทัด 934-941

อธิบายใต้รูป

- หน้า View แสดงปุ่มยืนยันรับสินค้าเมื่อออเดอร์อยู่ในสถานะที่พร้อมปิดรายการ
- ปุ่มนี้เป็นส่วนหนึ่งของหน้าติดตามสถานะคำสั่งซื้อ

### 14.4 ผลลัพธ์ที่ผู้ใช้เห็น

- ผู้ใช้สามารถกดยืนยันว่าได้รับสินค้าแล้วได้
- เมื่อยืนยันสำเร็จ ออเดอร์จะถูกย้ายไปอยู่ในประวัติคำสั่งซื้อ
- หน้า Delivery จะแสดงผลตามสถานะล่าสุดของรายการ

### 14.5 ตารางฐานข้อมูลที่เกี่ยวข้อง

- `Orders`
  ใช้อัปเดตสถานะคำสั่งซื้อเป็น `Completed`
- `Historyorders`
  ใช้บันทึกประวัติคำสั่งซื้อหลังผู้ใช้ยืนยันรับสินค้า
- `Addresses`
  ใช้นำข้อมูลที่อยู่จัดส่งไปบันทึกในประวัติ
- `Orderdetails`
  ใช้นำข้อมูลรายการสินค้าไปสรุปในประวัติคำสั่งซื้อ

---

## 15. ระบบการแจ้งเตือน

### 15.1 ViewModel ที่ใช้
ระบบการแจ้งเตือนไม่ได้ใช้ ViewModel แยกเฉพาะหน้า แต่ Controller ส่งข้อมูลกลับในรูปแบบ JSON จากตาราง `Notifications` และแสดงผลผ่านส่วน notification ใน `_Navbar`

รูปที่ควรแปะ

- `Controllers/NotificationController.cs` บรรทัด 15-38

อธิบายใต้รูป

- ระบบส่งข้อมูลแจ้งเตือนกลับเป็นรายการ JSON เช่น `id`, `type`, `title`, `message`, `href`, `isRead`
- ข้อมูลชุดนี้ถูกนำไปแสดงในเมนูแจ้งเตือนของผู้ใช้

### 15.2 Controller
การทำงานของระบบแจ้งเตือนอยู่ใน `NotificationController` ได้แก่ `GetMyNotifications()`, `MarkAsRead(int id)`, `MarkAllAsRead()` และ `ClearMyNotifications()`

รูปที่ควรแปะ

- `Controllers/NotificationController.cs` บรรทัด 15-93

ลำดับการทำงาน

1. ระบบเรียก `GetMyNotifications()` เพื่อดึงรายการแจ้งเตือนของผู้ใช้ปัจจุบัน
2. Controller ตรวจสอบ `UserId` จาก `Session` ก่อนทุกครั้ง
3. ถ้าผู้ใช้กดอ่านรายการเดียว ระบบจะเรียก `MarkAsRead(int id)` เพื่อเปลี่ยนสถานะเป็นอ่านแล้ว
4. ถ้าผู้ใช้กดอ่านทั้งหมด ระบบจะเรียก `MarkAllAsRead()`
5. ถ้าผู้ใช้ต้องการล้างรายการทั้งหมด ระบบจะเรียก `ClearMyNotifications()`
6. ทุก action จะส่งผลลัพธ์กลับเป็น JSON เพื่ออัปเดตการแสดงผลในหน้าเว็บ

### 15.3 View
ส่วนแสดงผลการแจ้งเตือนอยู่ใน `Views/Shared/_Navbar.cshtml`

รูปที่ควรแปะ

- `Views/Shared/_Navbar.cshtml` บรรทัด 390-411

อธิบายใต้รูป

- หน้า View มีไอคอนกระดิ่งสำหรับเปิดเมนูแจ้งเตือน
- มีส่วนแสดง badge จำนวนแจ้งเตือน และรายการแจ้งเตือนของผู้ใช้
- มีปุ่มสำหรับอ่านทั้งหมดและล้างรายการแจ้งเตือน

### 15.4 ผลลัพธ์ที่ผู้ใช้เห็น

- ผู้ใช้จะเห็นรายการแจ้งเตือนล่าสุดจากระบบได้ที่แถบนำทาง
- ผู้ใช้สามารถกดอ่านทีละรายการ อ่านทั้งหมด หรือล้างรายการทั้งหมดได้
- การแจ้งเตือนช่วยให้ผู้ใช้ติดตามออเดอร์ การชำระเงิน และโปรโมชันได้ง่ายขึ้น

### 15.5 ตารางฐานข้อมูลที่เกี่ยวข้อง

- `Notifications`
  ใช้เก็บข้อมูลแจ้งเตือนทั้งหมดของผู้ใช้
- `Users`
  ใช้อ้างอิงเจ้าของการแจ้งเตือนผ่าน `UserId`

---

## 16. ระบบจัดการออเดอร์

### 16.1 ViewModel ที่ใช้
ระบบจัดการออเดอร์ไม่ได้ใช้ ViewModel แยกเฉพาะหน้า แต่ Controller ส่งข้อมูลแบบ `List<Order>` ไปยัง View โดยตรง เพื่อใช้แสดงรายการออเดอร์ทั้งหมดของลูกค้า

รูปที่ควรแปะ

- `Controllers/Admin/AdminOrderController.cs` บรรทัด 21-36
- `Models/Db/Order.cs` บรรทัด 6-33

อธิบายใต้รูป

- หน้า Order ใช้ข้อมูลจาก `Order` โดยตรง
- ภายใน `Order` มีความสัมพันธ์กับ `User`, `Orderdetails` และ `Promotion` เพื่อใช้แสดงข้อมูลลูกค้า รายการสินค้า และโปรโมชันที่ใช้ในออเดอร์

### 16.2 Controller
การทำงานของระบบจัดการออเดอร์อยู่ใน `AdminOrderController.Order()` และ `AdminOrderController.GetOrderDetails(int orderId)`

รูปที่ควรแปะ

- `Controllers/Admin/AdminOrderController.cs` บรรทัด 21-36
- `Controllers/Admin/AdminOrderController.cs` บรรทัด 98-141

ลำดับการทำงาน

1. ระบบเปิดหน้า `Order()` เพื่อแสดงรายการออเดอร์ทั้งหมดให้พนักงานตรวจสอบ
2. Controller ตรวจสอบสิทธิ์ก่อนว่าเป็น `Admin` หรือ `Staff`
3. จากนั้นระบบดึงข้อมูลจากตาราง `Orders` พร้อมข้อมูลผู้ใช้ โปรโมชัน และรายการสินค้าในออเดอร์
4. ระบบเรียงออเดอร์ตามวันที่สั่งซื้อจากใหม่ไปเก่า
5. เมื่อพนักงานกดดูรายละเอียด ระบบจะเรียก `GetOrderDetails(int orderId)`
6. Controller จะดึงข้อมูลออเดอร์แบบละเอียดและส่งกลับเป็น JSON เพื่อใช้แสดงรายละเอียดเพิ่มเติมของคำสั่งซื้อ

### 16.3 View
หน้า Order อยู่ใน `Views/admin/Order.cshtml`

รูปที่ควรแปะ

- `Views/admin/Order.cshtml` บรรทัด 1-18
- `Views/admin/Order.cshtml` บรรทัด 638-719

อธิบายใต้รูป

- หน้า View ใช้ `@model List<Order>`
- มีการวนแสดงออเดอร์ด้วย `@foreach (var order in Model)`
- แต่ละรายการแสดงรหัสออเดอร์ ชื่อลูกค้า รายการสินค้า ยอดรวม สถานะ สลิป และปุ่มดูรายละเอียด

### 16.4 ผลลัพธ์ที่ผู้ใช้เห็น

- พนักงานจะเห็นรายการออเดอร์ทั้งหมดของลูกค้าในหน้าเดียว
- พนักงานสามารถตรวจสอบข้อมูลลูกค้า รายการสินค้า ยอดเงิน และสถานะปัจจุบันของแต่ละออเดอร์ได้
- พนักงานสามารถกดดูรายละเอียดออเดอร์แต่ละรายการเพื่ออ่านข้อมูลเพิ่มเติมก่อนดำเนินการขั้นถัดไป

### 16.5 ตารางฐานข้อมูลที่เกี่ยวข้อง

- `Orders`
  ใช้เก็บข้อมูลหลักของคำสั่งซื้อ เช่น ผู้ใช้ ยอดรวม สถานะออเดอร์ และสถานะการชำระเงิน
- `Orderdetails`
  ใช้เก็บรายการสินค้าแต่ละชิ้นภายในออเดอร์
- `Users`
  ใช้อ้างอิงข้อมูลลูกค้าที่เป็นเจ้าของออเดอร์
- `Promotions`
  ใช้อ้างอิงโปรโมชันที่ถูกใช้กับออเดอร์

---

## 17. ระบบยืนยันการชำระเงิน

### 17.1 ViewModel ที่ใช้
ระบบยืนยันการชำระเงินไม่ได้ใช้ ViewModel แยกเฉพาะ แต่ทำงานร่วมกับข้อมูล `Order` และ `Orderdetails` ในหน้าจัดการออเดอร์

รูปที่ควรแปะ

- `Models/Db/Order.cs` บรรทัด 6-33
- `Models/Db/Orderdetail.cs` บรรทัด 6-20

อธิบายใต้รูป

- ระบบใช้ข้อมูลออเดอร์เพื่อตรวจสอบสถานะการชำระเงินและสลิปที่ลูกค้าอัปโหลด
- ใช้รายการสินค้าใน `Orderdetails` เพื่ออ้างอิงจำนวนสินค้าที่ต้องตัดสต็อกหลังยืนยันการชำระเงิน

### 17.2 Controller
การทำงานของระบบยืนยันการชำระเงินอยู่ใน `AdminOrderController.ConfirmPayment(int orderId)`

รูปที่ควรแปะ

- `Controllers/Admin/AdminOrderController.cs` บรรทัด 41-93

ลำดับการทำงาน

1. เมื่อพนักงานกดปุ่มยืนยันการชำระเงิน ระบบจะเรียก `ConfirmPayment(int orderId)`
2. Controller ตรวจสอบสิทธิ์ก่อนว่าเป็น `Admin` หรือ `Staff`
3. จากนั้นระบบดึงข้อมูลออเดอร์พร้อมรายการสินค้าในออเดอร์นั้น
4. Controller วนตรวจสอบสินค้าแต่ละรายการเพื่อตัดจำนวนสต็อกตามจำนวนที่ลูกค้าสั่ง
5. ระบบอัปเดตสถานะการชำระเงินเป็น `Paid` และเปลี่ยนสถานะออเดอร์เป็น `Paid`
6. หลังจากนั้นระบบบันทึกการแจ้งเตือนไปยังลูกค้า
7. สุดท้ายระบบส่งผลลัพธ์กลับเป็น JSON เพื่อแจ้งว่าการยืนยันสำเร็จหรือไม่สำเร็จ

### 17.3 View
ส่วนยืนยันการชำระเงินอยู่ใน `Views/admin/Order.cshtml`

รูปที่ควรแปะ

- `Views/admin/Order.cshtml` บรรทัด 682-703

อธิบายใต้รูป

- หน้า View แสดงรูปสลิปการโอนเงินของลูกค้าในตารางออเดอร์
- ถ้าออเดอร์อยู่ในสถานะ `PendingVerify` จะแสดงปุ่มยืนยันการชำระเงินให้พนักงานดำเนินการ

### 17.4 ผลลัพธ์ที่ผู้ใช้เห็น

- พนักงานสามารถเปิดดูสลิปของลูกค้าและตรวจสอบการโอนเงินได้
- เมื่อยืนยันสำเร็จ สถานะการชำระเงินของออเดอร์จะเปลี่ยนเป็นชำระแล้ว
- ระบบจะตัดสต็อกสินค้าและแจ้งผลไปยังลูกค้า

### 17.5 ตารางฐานข้อมูลที่เกี่ยวข้อง

- `Orders`
  ใช้อัปเดตสถานะการชำระเงินและสถานะออเดอร์หลังตรวจสอบสลิป
- `Orderdetails`
  ใช้อ้างอิงจำนวนสินค้าที่ต้องนำไปตัดสต็อก
- `Stocks`
  ใช้อัปเดตจำนวนคงเหลือของสินค้าเมื่อยืนยันการชำระเงิน
- `Notifications`
  ใช้บันทึกการแจ้งเตือนให้ลูกค้าทราบว่าระบบยืนยันการชำระเงินแล้ว

---

## 18. ระบบเตรียมสินค้าและจัดส่ง

### 18.1 ViewModel ที่ใช้
ระบบเตรียมสินค้าและจัดส่งไม่ได้ใช้ ViewModel แยกเฉพาะ แต่ทำงานร่วมกับข้อมูล `Order` ในหน้าจัดการออเดอร์

รูปที่ควรแปะ

- `Models/Db/Order.cs` บรรทัด 6-33

อธิบายใต้รูป

- ระบบใช้ข้อมูล `Order.Status` เพื่อตรวจสอบว่าออเดอร์อยู่ในขั้นตอนใด
- ข้อมูลเดียวกันนี้ถูกนำไปใช้แสดงปุ่มรับออเดอร์และปุ่มจัดส่งสินค้าในหน้า View

### 18.2 Controller
การทำงานของระบบเตรียมสินค้าและจัดส่งอยู่ใน `AdminOrderController.AcceptOrder(int orderId)` และ `AdminOrderController.ShipOrder(int orderId)`

รูปที่ควรแปะ

- `Controllers/Admin/AdminOrderController.cs` บรรทัด 161-203
- `Controllers/Admin/AdminOrderController.cs` บรรทัด 207-246

ลำดับการทำงาน

1. เมื่อพนักงานกดรับออเดอร์ ระบบจะเรียก `AcceptOrder(int orderId)`
2. Controller ตรวจสอบสิทธิ์และค้นหาออเดอร์ที่ต้องการเปลี่ยนสถานะ
3. ถ้าออเดอร์ยังไม่อยู่ในขั้นตอนที่ถูกดำเนินการไปแล้ว ระบบจะเปลี่ยนสถานะเป็น `Preparing`
4. จากนั้นระบบบันทึกการแจ้งเตือนให้ลูกค้าทราบว่าออเดอร์กำลังถูกเตรียม
5. เมื่อพนักงานกดจัดส่ง ระบบจะเรียก `ShipOrder(int orderId)`
6. Controller ตรวจสอบว่าออเดอร์อยู่ในสถานะ `Preparing` ก่อน
7. ถ้าถูกต้อง ระบบจะเปลี่ยนสถานะเป็น `Shipped` และบันทึกการแจ้งเตือนให้ลูกค้า

### 18.3 View
ส่วนเตรียมสินค้าและจัดส่งอยู่ใน `Views/admin/Order.cshtml`

รูปที่ควรแปะ

- `Views/admin/Order.cshtml` บรรทัด 699-715

อธิบายใต้รูป

- หน้า View แสดงปุ่มตามสถานะของออเดอร์
- ถ้าออเดอร์อยู่ในสถานะ `Paid` จะแสดงปุ่มรับออเดอร์
- ถ้าออเดอร์อยู่ในสถานะ `Preparing` จะแสดงปุ่มจัดส่งสินค้า

### 18.4 ผลลัพธ์ที่ผู้ใช้เห็น

- พนักงานสามารถเปลี่ยนสถานะออเดอร์จากชำระเงินแล้ว ไปเป็นกำลังเตรียมสินค้า และไปเป็นจัดส่งได้
- ลูกค้าจะเห็นสถานะออเดอร์อัปเดตตามขั้นตอนจริงของร้าน
- ระบบช่วยให้ติดตามความคืบหน้าของการจัดเตรียมและจัดส่งสินค้าได้ชัดเจนขึ้น

### 18.5 ตารางฐานข้อมูลที่เกี่ยวข้อง

- `Orders`
  ใช้อัปเดตสถานะคำสั่งซื้อจาก `Paid` เป็น `Preparing` และ `Shipped`
- `Notifications`
  ใช้บันทึกการแจ้งเตือนเมื่อออเดอร์เข้าสู่ขั้นตอนเตรียมสินค้าและจัดส่ง

---

## 19. ระบบจัดการสต็อกสินค้า

### 19.1 ViewModel ที่ใช้
ระบบจัดการสต็อกสินค้าใช้ `AdminStockViewModel` เพื่อรวมข้อมูลหมวดหมู่สินค้าและรายการสินค้าไว้ในหน้าเดียว

รูปที่ควรแปะ

- `Viewmodels/AdminStockViewModel.cs` บรรทัด 1-8

อธิบายใต้รูป

- `AdminStockViewModel` มี `Categories` สำหรับแสดงหมวดหมู่สินค้า
- มี `Products` สำหรับแสดงรายการสินค้าและข้อมูลสต็อกของแต่ละรายการ

### 19.2 Controller
การทำงานของระบบจัดการสต็อกสินค้าอยู่ใน `AdminStockController.Stock()` และ `AdminStockController.UpdateStockSettings(...)`

รูปที่ควรแปะ

- `Controllers/Admin/AdminStockController.cs` บรรทัด 17-35
- `Controllers/Admin/AdminStockController.cs` บรรทัด 37-63

ลำดับการทำงาน

1. ระบบเปิดหน้า `Stock()` เพื่อแสดงข้อมูลสินค้าและสต็อกทั้งหมด
2. Controller ตรวจสอบสิทธิ์ก่อนว่าเป็น `Admin` หรือ `Staff`
3. จากนั้นระบบดึงข้อมูลหมวดหมู่จากตาราง `Categories` และดึงข้อมูลสินค้าจากตาราง `Stocks`
4. Controller รวมข้อมูลทั้งหมดไว้ใน `AdminStockViewModel`
5. เมื่อพนักงานปรับจำนวนสต็อกหรือเปลี่ยนสถานะขาย ระบบจะเรียก `UpdateStockSettings(...)`
6. Controller คำนวณจำนวนสต็อกใหม่ พร้อมอัปเดต `IsAvailable` และ `IsLimitedQuantity`
7. สุดท้ายระบบบันทึกผลและกลับมาแสดงหน้า Stock อีกครั้ง

### 19.3 View
หน้า Stock อยู่ใน `Views/admin/Stock.cshtml`

รูปที่ควรแปะ

- `Views/admin/Stock.cshtml` บรรทัด 4-8
- `Views/admin/Stock.cshtml` บรรทัด 120-137
- `Views/admin/Stock.cshtml` บรรทัด 167-225
- `Views/admin/Stock.cshtml` บรรทัด 296-308

อธิบายใต้รูป

- หน้า View ใช้ `@model AdminStockViewModel`
- มีการวนแสดงหมวดหมู่ด้วย `@foreach (var category in Model.Categories)`
- มีการวนแสดงรายการสินค้าด้วย `@foreach (var product in Model.Products)`
- มีฟอร์ม `UpdateStockSettings` สำหรับปรับจำนวนสต็อก เปิดขายหรือปิดขาย และกำหนดว่าสินค้าเป็นสินค้าจำกัดจำนวน

### 19.4 ผลลัพธ์ที่ผู้ใช้เห็น

- พนักงานจะเห็นรายการสินค้า หมวดหมู่ ราคา จำนวนคงเหลือ และสถานะขายของสินค้าแต่ละรายการ
- พนักงานสามารถปรับสต็อกสินค้าและสถานะการขายได้จากหน้าเดียว
- ระบบช่วยให้พนักงานตรวจสอบสินค้าหมดสต็อก ใกล้หมด หรือสินค้าที่ปิดขายได้ง่ายขึ้น

### 19.5 ตารางฐานข้อมูลที่เกี่ยวข้อง

- `Stocks`
  ใช้เก็บข้อมูลสินค้า ราคา จำนวนคงเหลือ และสถานะพร้อมขาย
- `Categories`
  ใช้เก็บหมวดหมู่ของสินค้าเพื่อจัดกลุ่มรายการในหน้า Stock

---

## 20. ระบบตรวจสอบคำขอโปรโมชัน

### 20.1 ViewModel ที่ใช้
ระบบตรวจสอบคำขอโปรโมชันใช้ `AdminPromotionViewModel` เพื่อรวมข้อมูลโปรโมชัน ผู้ใช้ สินค้า และรายการคำขอที่รอตรวจสอบไว้ในหน้าเดียว

รูปที่ควรแปะ

- `Viewmodels/AdminPromotionViewModel.cs` บรรทัด 1-11

อธิบายใต้รูป

- `AdminPromotionViewModel` มี `PendingClaims` สำหรับรายการคำขอโปรโมชันที่รอการตรวจสอบ
- มี `Promotions` และ `Stocks` เพื่อใช้อ้างอิงข้อมูลโปรโมชันและของรางวัลที่เกี่ยวข้อง

### 20.2 Controller
การทำงานของระบบตรวจสอบคำขอโปรโมชันอยู่ใน `AdminPromotionController.EventPromotion()`, `ApproveClaim(int claimId, string? returnTo)` และ `RejectClaim(int claimId, string? reviewNote, string? returnTo)`

รูปที่ควรแปะ

- `Controllers/Admin/AdminPromotionController.cs` บรรทัด 33-37
- `Controllers/Admin/AdminPromotionController.cs` บรรทัด 307-355
- `Controllers/Admin/AdminPromotionController.cs` บรรทัด 357-405

ลำดับการทำงาน

1. ระบบเปิดหน้า `EventPromotion()` เพื่อแสดงคำขอโปรโมชันกิจกรรมที่รอการตรวจสอบ
2. Controller ตรวจสอบสิทธิ์ก่อนว่าเป็น `Admin` หรือ `Staff`
3. จากนั้นระบบสร้าง `AdminPromotionViewModel` และส่งไปยังหน้า View
4. เมื่อพนักงานกดอนุมัติ ระบบจะเรียก `ApproveClaim(...)`
5. Controller ตรวจสอบคำขอ จากนั้นเปลี่ยนสถานะเป็น `Approved` และเพิ่มสิทธิ์โปรโมชันให้ผู้ใช้ใน `UserPromotions`
6. เมื่อพนักงานกดปฏิเสธ ระบบจะเรียก `RejectClaim(...)`
7. Controller จะเปลี่ยนสถานะคำขอเป็น `Rejected` บันทึกเหตุผล และส่งการแจ้งเตือนกลับไปยังผู้ใช้

### 20.3 View
หน้า Event Promotion อยู่ใน `Views/admin/EventPromotion.cshtml`

รูปที่ควรแปะ

- `Views/admin/EventPromotion.cshtml` บรรทัด 1-10
- `Views/admin/EventPromotion.cshtml` บรรทัด 154-185
- `Views/admin/EventPromotion.cshtml` บรรทัด 207-245

อธิบายใต้รูป

- หน้า View ใช้ `@model AdminPromotionViewModel`
- มีส่วนแสดงจำนวนคำขอที่รอตรวจสอบจาก `@Model.PendingClaims.Count`
- มีการวนแสดงรายการคำขอด้วย `@foreach (var claim in Model.PendingClaims)`
- แต่ละรายการมีปุ่มอนุมัติและปุ่มปฏิเสธเพื่อให้พนักงานดำเนินการได้ทันที

### 20.4 ผลลัพธ์ที่ผู้ใช้เห็น

- พนักงานจะเห็นรายการคำขอโปรโมชันกิจกรรมที่ลูกค้าส่งเข้ามาพร้อมหลักฐาน
- พนักงานสามารถตรวจสอบรูปหลักฐาน รายละเอียดคำขอ และกดอนุมัติหรือปฏิเสธได้จากหน้าเดียว
- เมื่อดำเนินการแล้ว ลูกค้าจะได้รับสิทธิ์โปรโมชันหรือได้รับการแจ้งผลการปฏิเสธตามที่พนักงานตัดสิน

### 20.5 ตารางฐานข้อมูลที่เกี่ยวข้อง

- `PromotionClaims`
  ใช้เก็บคำขอรับโปรโมชัน หลักฐานที่ส่งมา และสถานะการตรวจสอบ
- `Promotions`
  ใช้อ้างอิงข้อมูลโปรโมชันที่ลูกค้ากำลังขอรับสิทธิ์
- `UserPromotions`
  ใช้บันทึกสิทธิ์โปรโมชันที่ได้รับหลังจากอนุมัติคำขอ
- `Notifications`
  ใช้ส่งผลการอนุมัติหรือปฏิเสธกลับไปยังลูกค้า

---

## 21. ระบบแดชบอร์ดผู้ดูแลระบบ

### 21.1 ViewModel ที่ใช้
ระบบแดชบอร์ดผู้ดูแลระบบใช้ `AdminDashboardViewModel` สำหรับรวมข้อมูลสรุปที่ต้องแสดงบนหน้า Dashboard เช่น รายได้ จำนวนออเดอร์ สินค้าใกล้หมด และข้อมูลสถิติรายเดือน

รูปที่ควรแปะ

- `Models/AdminDashboardViewModel.cs` หรือไฟล์ ViewModel ที่เกี่ยวข้อง บรรทัด 1-40

อธิบายใต้รูป

- `AdminDashboardViewModel` ใช้เก็บค่า KPI หลักของระบบ
- มีข้อมูลเช่น `TodayRevenue`, `TodayOrders`, `LowStockProducts`, `OutOfStockProducts`
- มีรายการย่อยสำหรับกราฟรายได้ รายการออเดอร์ล่าสุด และสินค้าที่ใกล้หมด

### 21.2 Controller
การทำงานของระบบแดชบอร์ดอยู่ใน `AdminDashboardController.Dashbordadmin()`

รูปที่ควรแปะ

- `Controllers/Admin/AdminDashboardController.cs` บรรทัด 12-68
- `Controllers/Admin/AdminDashboardController.cs` บรรทัด 69-119

ลำดับการทำงาน

1. ระบบเปิดหน้า `Dashbordadmin()` เพื่อแสดงภาพรวมของร้านในมุมมองผู้ดูแลระบบ
2. Controller ตรวจสอบสิทธิ์ก่อนว่าเป็น `Admin` เท่านั้น
3. จากนั้นระบบดึงข้อมูลออเดอร์ทั้งหมดพร้อมข้อมูลผู้ใช้จากตาราง `Orders`
4. ระบบดึงข้อมูลสินค้าและหมวดหมู่จากตาราง `Stocks` และ `Categories`
5. Controller คำนวณข้อมูลสรุป เช่น รายได้ประจำวัน จำนวนออเดอร์ที่รอตรวจสอบ ยอดผู้ใช้ทั้งหมด และจำนวนสินค้าที่ใกล้หมด
6. ระบบสร้างข้อมูลรายได้ย้อนหลัง 6 เดือน รายการออเดอร์ล่าสุด และสรุปจำนวนสินค้าตามหมวดหมู่
7. สุดท้าย Controller รวมข้อมูลทั้งหมดไว้ใน `AdminDashboardViewModel` และส่งไปยังหน้า View

### 21.3 View
หน้า Dashboard อยู่ใน `Views/admin/Dashbordadmin.cshtml`

รูปที่ควรแปะ

- `Views/admin/Dashbordadmin.cshtml` บรรทัด 1-22
- `Views/admin/Dashbordadmin.cshtml` บรรทัด 430-443
- `Views/admin/Dashbordadmin.cshtml` บรรทัด 445-478
- `Views/admin/Dashbordadmin.cshtml` บรรทัด 492-507
- `Views/admin/Dashbordadmin.cshtml` บรรทัด 570-579
- `Views/admin/Dashbordadmin.cshtml` บรรทัด 595-606
- `Views/admin/Dashbordadmin.cshtml` บรรทัด 632-637

อธิบายใต้รูป

- หน้า View ใช้ `@model AdminDashboardViewModel`
- มีส่วนแสดงตัวเลขสรุป เช่น รายได้ ออเดอร์ และจำนวนผู้ใช้
- มีส่วนกราฟหรือรายการสรุปเพื่อช่วยให้แอดมินเห็นแนวโน้มของร้าน

### 21.4 ผลลัพธ์ที่ผู้ใช้เห็น

- แอดมินจะเห็นหน้า Dashboard ที่รวมข้อมูลสำคัญของร้านในหน้าเดียว
- สามารถดูยอดขาย ออเดอร์ล่าสุด สินค้าใกล้หมด และสถิติภาพรวมได้ทันที
- ช่วยให้ผู้ดูแลระบบตัดสินใจเรื่องสต็อก โปรโมชัน และการติดตามยอดขายได้ง่ายขึ้น

### 21.5 ตารางฐานข้อมูลที่เกี่ยวข้อง

- `Orders`
  ใช้คำนวณยอดขาย จำนวนออเดอร์ และสถานะคำสั่งซื้อ
- `Users`
  ใช้นับจำนวนผู้ใช้ทั้งหมดในระบบ
- `Stocks`
  ใช้ตรวจสอบจำนวนคงเหลือของสินค้า
- `Categories`
  ใช้สรุปจำนวนสินค้าแยกตามหมวดหมู่
- `Promotions`
  ใช้นับจำนวนโปรโมชันที่มีอยู่ในระบบ

---

## 22. ระบบจัดการสมาชิก

### 22.1 ViewModel ที่ใช้
ระบบจัดการสมาชิกไม่ได้ใช้ ViewModel แยกเฉพาะหน้า แต่ส่งรายการผู้ใช้จาก `Db.Users` ไปยัง View โดยตรง และใช้ `ViewBag.Roles` สำหรับส่งรายการบทบาทของผู้ใช้ไปสร้างตัวเลือกในฟอร์ม

รูปที่ควรแปะ

- `Controllers/Admin/AdminMemberController.cs` บรรทัด 15-31

อธิบายใต้รูป

- หน้า Member ใช้ข้อมูล `Users` เป็น model หลัก
- ใช้ `ViewBag.Roles` เพื่อแสดงตัวเลือกสิทธิ์ของผู้ใช้ในฟอร์มเพิ่มหรือแก้ไขข้อมูล

### 22.2 Controller
การทำงานของระบบจัดการสมาชิกอยู่ใน `AdminMemberController.Member()`, `SaveMember(...)` และ `DeleteMember(int userId)`

รูปที่ควรแปะ

- `Controllers/Admin/AdminMemberController.cs` บรรทัด 15-31
- `Controllers/Admin/AdminMemberController.cs` บรรทัด 34-99
- `Controllers/Admin/AdminMemberController.cs` บรรทัด 102-133

ลำดับการทำงาน

1. ระบบเปิดหน้า `Member()` เพื่อแสดงรายการสมาชิกทั้งหมดในระบบ
2. Controller ตรวจสอบสิทธิ์ก่อนว่าเป็น `Admin` เท่านั้น
3. จากนั้นระบบดึงข้อมูลบทบาทจากตาราง `Roles` และดึงข้อมูลผู้ใช้จากตาราง `Users`
4. เมื่อแอดมินเพิ่มหรือแก้ไขสมาชิก ระบบจะเรียก `SaveMember(...)`
5. Controller ตรวจสอบข้อมูลสำคัญ เช่น `username` และ `roleId`
6. ถ้าเป็นการแก้ไข ระบบจะอัปเดตข้อมูลผู้ใช้เดิม และสามารถเปลี่ยนรหัสผ่านได้ถ้ามีการกรอกค่าใหม่
7. ถ้าเป็นการเพิ่มสมาชิกใหม่ ระบบจะสร้างข้อมูลผู้ใช้ใหม่พร้อมกำหนดบทบาท
8. เมื่อแอดมินลบสมาชิก ระบบจะเรียก `DeleteMember(userId)`
9. Controller จะป้องกันไม่ให้แอดมินลบบัญชีของตัวเองที่กำลังใช้งานอยู่
10. ถ้าตรวจสอบผ่าน ระบบจะลบผู้ใช้ออกจากฐานข้อมูลและ redirect กลับหน้า Member

### 22.3 View
หน้าจัดการสมาชิกอยู่ใน `Views/admin/Member.cshtml`

รูปที่ควรแปะ

- `Views/admin/Member.cshtml` บรรทัด 1-8
- `Views/admin/Member.cshtml` บรรทัด 453-485
- `Views/admin/Member.cshtml` บรรทัด 487-525
- `Views/admin/Member.cshtml` บรรทัด 528-605
- `Views/admin/Member.cshtml` บรรทัด 623-671
- `Views/admin/Member.cshtml` บรรทัด 677-755

อธิบายใต้รูป

- หน้า View แสดงรายการสมาชิกทั้งหมดพร้อมบทบาทของแต่ละคน
- มีฟอร์มสำหรับเพิ่มสมาชิกใหม่หรือแก้ไขข้อมูลเดิม
- มีปุ่มลบสมาชิกพร้อมกล่องยืนยันก่อนลบ

### 22.4 ผลลัพธ์ที่ผู้ใช้เห็น

- แอดมินจะเห็นรายชื่อผู้ใช้ทั้งหมดในระบบพร้อมบทบาทของแต่ละบัญชี
- สามารถเพิ่ม แก้ไข หรือเปลี่ยนสิทธิ์ของผู้ใช้ได้จากหน้าเดียว
- ระบบช่วยป้องกันการลบบัญชีแอดมินที่กำลังใช้งานอยู่

### 22.5 ตารางฐานข้อมูลที่เกี่ยวข้อง

- `Users`
  ใช้เก็บข้อมูลบัญชีผู้ใช้ เช่น ชื่อผู้ใช้ อีเมล เบอร์โทร รหัสผ่าน และบทบาท
- `Roles`
  ใช้เก็บประเภทสิทธิ์ของผู้ใช้ เช่น Admin, Staff และ User

---

## 23. ระบบจัดการสินค้าและหมวดหมู่ของผู้ดูแลระบบ

### 23.1 ViewModel ที่ใช้
ระบบจัดการสินค้าและหมวดหมู่ใช้ `AdminStockViewModel` เช่นเดียวกับหน้าสต็อก แต่ในส่วนของแอดมินจะครอบคลุมการเพิ่มหรือแก้ไขสินค้าและหมวดหมู่เพิ่มเติมจากการปรับสต็อกทั่วไป

รูปที่ควรแปะ

- `Models/AdminStockViewModel.cs` หรือไฟล์ ViewModel ที่เกี่ยวข้อง บรรทัด 1-20

อธิบายใต้รูป

- `AdminStockViewModel` มี `Categories` สำหรับใช้แสดงและเลือกหมวดหมู่สินค้า
- มี `Products` สำหรับแสดงรายการสินค้าเดิมที่สามารถนำมาแก้ไขได้

### 23.2 Controller
การทำงานของระบบนี้อยู่ใน `AdminStockController.Stock()`, `SaveCategory(...)` และ `SaveStock(...)`

รูปที่ควรแปะ

- `Controllers/Admin/AdminStockController.cs` บรรทัด 15-29
- `Controllers/Admin/AdminStockController.cs` บรรทัด 54-91
- `Controllers/Admin/AdminStockController.cs` บรรทัด 94-192

ลำดับการทำงาน

1. ระบบเปิดหน้า `Stock()` เพื่อแสดงรายการสินค้าและหมวดหมู่ทั้งหมด
2. Controller ตรวจสอบสิทธิ์ก่อน และเปิดให้ `Admin` กับ `Staff` เข้าหน้านี้ได้
3. แต่ในส่วนการเพิ่มหรือแก้ไขหมวดหมู่ด้วย `SaveCategory(...)` ระบบอนุญาตเฉพาะ `Admin`
4. Controller ตรวจสอบก่อนว่าชื่อหมวดหมู่ถูกกรอกครบหรือไม่
5. ถ้า `categoryId > 0` ระบบจะอัปเดตหมวดหมู่เดิม แต่ถ้าไม่มีจะสร้างหมวดหมู่ใหม่
6. เมื่อแอดมินเพิ่มหรือแก้ไขสินค้า ระบบจะเรียก `SaveStock(...)`
7. Controller ตรวจสอบชื่อสินค้า หมวดหมู่ ราคา จำนวนสต็อก และชนิดไฟล์รูปภาพก่อนบันทึก
8. ถ้าเป็นการแก้ไข ระบบจะอัปเดตข้อมูลสินค้าเดิม พร้อมอัปโหลดรูปใหม่ได้
9. ถ้าเป็นการเพิ่มสินค้าใหม่ ระบบจะสร้างข้อมูลใหม่ในตาราง `Stocks`
10. สุดท้ายระบบบันทึกผลและ redirect กลับหน้า Stock

### 23.3 View
หน้าใช้งานอยู่ใน `Views/admin/Stock.cshtml`

รูปที่ควรแปะ

- `Views/admin/Stock.cshtml` บรรทัด 1-4
- `Views/admin/Stock.cshtml` บรรทัด 120-140
- `Views/admin/Stock.cshtml` บรรทัด 167-225
- `Views/admin/Stock.cshtml` บรรทัด 244-251
- `Views/admin/Stock.cshtml` บรรทัด 266-280

อธิบายใต้รูป

- หน้า View เดียวกันรองรับทั้งการดูสต็อก การเพิ่มหมวดหมู่ และการเพิ่มสินค้า
- มีฟอร์มแยกสำหรับหมวดหมู่และฟอร์มแยกสำหรับสินค้า
- รองรับการแนบรูปสินค้าและกำหนดสถานะพร้อมขาย

### 23.4 ผลลัพธ์ที่ผู้ใช้เห็น

- แอดมินสามารถเพิ่มหมวดหมู่ใหม่และแก้ไขหมวดหมู่เดิมได้
- สามารถเพิ่มสินค้าใหม่หรือแก้ไขข้อมูลสินค้าเดิมได้จากหน้าเดียวกัน
- ระบบช่วยให้การจัดการข้อมูลหน้าร้านและข้อมูลสต็อกอยู่ในจุดเดียว

### 23.5 ตารางฐานข้อมูลที่เกี่ยวข้อง

- `Categories`
  ใช้เก็บชื่อหมวดหมู่และคำอธิบายของหมวดหมู่สินค้า
- `Stocks`
  ใช้เก็บข้อมูลสินค้า ราคา สต็อก รูปภาพ และสถานะพร้อมขาย

---

## 24. ระบบจัดการโปรโมชันของผู้ดูแลระบบ

### 24.1 ViewModel ที่ใช้
ระบบจัดการโปรโมชันใช้ `AdminPromotionViewModel` สำหรับรวมข้อมูลโปรโมชัน ผู้ใช้ สินค้า รายการคำขอ และข้อมูลที่ใช้ในฟอร์มสร้างหรือแก้ไขโปรโมชันไว้ในหน้าเดียว

รูปที่ควรแปะ

- `Viewmodels/AdminPromotionViewModel.cs` บรรทัด 1-20

อธิบายใต้รูป

- `AdminPromotionViewModel` มีรายการโปรโมชันทั้งหมดสำหรับแสดงบนหน้าแอดมิน
- มีข้อมูลสินค้าและผู้ใช้สำหรับใช้ในฟอร์มเลือกของแถมหรือการมอบโปรโมชัน

### 24.2 Controller
การทำงานของระบบจัดการโปรโมชันอยู่ใน `AdminPromotionController.PromotionAdmin()`, `SavePromotion(...)`, `GiftPromotion(...)` และ `GiftPromotionToAll(int promotionId)`

รูปที่ควรแปะ

- `Controllers/Admin/AdminPromotionController.cs` บรรทัด 18-29
- `Controllers/Admin/AdminPromotionController.cs` บรรทัด 40-191
- `Controllers/Admin/AdminPromotionController.cs` บรรทัด 194-302

ลำดับการทำงาน

1. ระบบเปิดหน้า `PromotionAdmin()` เพื่อแสดงรายการโปรโมชันทั้งหมดในระบบ
2. Controller ตรวจสอบสิทธิ์ก่อนว่าเป็น `Admin` เท่านั้น
3. จากนั้นระบบสร้าง `AdminPromotionViewModel` เพื่อรวบรวมข้อมูลโปรโมชัน สินค้า ผู้ใช้ และคำขอที่เกี่ยวข้อง
4. เมื่อแอดมินสร้างหรือแก้ไขโปรโมชัน ระบบจะเรียก `SavePromotion(...)`
5. Controller ตรวจสอบชื่อโปรโมชัน ประเภทโปรโมชัน วันที่เริ่มและสิ้นสุด รวมถึงข้อมูลส่วนลดหรือของแถมให้ถูกต้อง
6. ถ้ามีการอัปโหลดรูป ระบบจะบันทึกรูปไว้ในโฟลเดอร์ `wwwroot/uploads/promotions`
7. ถ้าเป็นการแก้ไข ระบบจะอัปเดตข้อมูลโปรโมชันเดิมและรายการของรางวัลที่เกี่ยวข้อง
8. ถ้าเป็นการเพิ่มใหม่ ระบบจะสร้างข้อมูลโปรโมชันใหม่ลงในตาราง `Promotions`
9. เมื่อแอดมินต้องการมอบโปรโมชันให้ผู้ใช้เฉพาะราย ระบบจะเรียก `GiftPromotion(promotionId, userId)`
10. Controller จะตรวจสอบว่าโปรโมชันใช้งานได้และสร้างหรือรีเซ็ตสิทธิ์ใน `UserPromotions`
11. ถ้าต้องการมอบโปรโมชันให้ผู้ใช้ทั้งหมด ระบบจะเรียก `GiftPromotionToAll(promotionId)` และวนมอบสิทธิ์ให้ทุกบัญชี
12. หลังจากมอบโปรโมชัน ระบบจะสร้างการแจ้งเตือนไปยังผู้ใช้ที่ได้รับสิทธิ์

### 24.3 View
หน้าจัดการโปรโมชันอยู่ใน `Views/admin/PromotionAdmin.cshtml`

รูปที่ควรแปะ

- `Views/admin/PromotionAdmin.cshtml` บรรทัด 1-4
- `Views/admin/PromotionAdmin.cshtml` บรรทัด 394-518
- `Views/admin/PromotionAdmin.cshtml` บรรทัด 521-540
- `Views/admin/PromotionAdmin.cshtml` บรรทัด 695-715
- `Views/admin/PromotionAdmin.cshtml` บรรทัด 916-945

อธิบายใต้รูป

- หน้า View ใช้ `@model AdminPromotionViewModel`
- มีส่วนแสดงรายการโปรโมชันพร้อมรายละเอียดและสถานะการใช้งาน
- มีฟอร์มสร้างหรือแก้ไขโปรโมชันหลายประเภท เช่น ส่วนลด ของแถม และโปรโมชันกิจกรรม
- มีส่วนแจกโปรโมชันให้ผู้ใช้รายบุคคลหรือผู้ใช้ทั้งหมดได้จากหน้าเดียว

### 24.4 ผลลัพธ์ที่ผู้ใช้เห็น

- แอดมินจะเห็นรายการโปรโมชันทั้งหมดและจัดการได้จากหน้าเดียว
- สามารถสร้าง แก้ไข เปิดหรือปิดใช้งานโปรโมชัน และกำหนดของรางวัลได้
- สามารถมอบโปรโมชันให้ผู้ใช้เฉพาะคนหรือทั้งระบบ พร้อมส่งการแจ้งเตือนไปยังผู้ใช้ได้ทันที

### 24.5 ตารางฐานข้อมูลที่เกี่ยวข้อง

- `Promotions`
  ใช้เก็บข้อมูลหลักของโปรโมชัน เช่น ชื่อ ประเภท ส่วนลด วันเริ่มต้น และวันสิ้นสุด
- `PromotionRewardItems`
  ใช้เก็บรายการของรางวัลหรือของแถมที่ผูกกับโปรโมชัน
- `UserPromotions`
  ใช้เก็บสิทธิ์โปรโมชันที่ถูกมอบให้ผู้ใช้แต่ละคน
- `Users`
  ใช้อ้างอิงผู้ใช้ที่ได้รับสิทธิ์โปรโมชัน
- `Stocks`
  ใช้อ้างอิงสินค้าในกรณีที่โปรโมชันมีของแถมหรือรางวัล
- `Notifications`
  ใช้ส่งข้อความแจ้งเตือนเมื่อมีการมอบโปรโมชันให้ผู้ใช้
